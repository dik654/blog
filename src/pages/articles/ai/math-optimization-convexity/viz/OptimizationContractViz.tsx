import VizFrame from "@/components/viz/VizFrame";
const items=[["목표","min f(x)","무엇을 좋다고 볼 것인가"],["구조","convex · smooth?","어떤 보장이 가능한가"],["규칙","x−η∇f(x)","어떻게 다음 점을 고를 것인가"],["종료","gap · norm · budget","언제 충분하다고 멈출 것인가"]] as const;
export default function OptimizationContractViz(){return <VizFrame eyebrow="문제 계약" title="Gradient 하나만으로 optimization 문제는 완성되지 않습니다"><div className="grid gap-px overflow-hidden rounded-lg border border-border/70 bg-border/60 md:grid-cols-4">{items.map(([title,code,detail],i)=><div key={title} className="min-w-0 bg-background p-5"><p className="font-mono text-[10px] font-bold text-primary">0{i+1} · {title}</p><p className="mt-5 break-words font-mono text-sm font-bold">{code}</p><p className="mt-3 text-xs leading-5 text-muted-foreground">{detail}</p></div>)}</div></VizFrame>}
