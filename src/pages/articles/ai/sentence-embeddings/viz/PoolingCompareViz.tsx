const methods=[["CLS","special token state","checkpoint가 그렇게 학습됐는지 확인"],["Mean","valid token average","padding mask와 length effect 확인"],["Weighted","position·token weight","추가 가정과 latency 확인"]];
export default function PoolingCompareViz(){return <div data-viz className="rounded-2xl border border-border/70 bg-card/70 p-4 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-600 dark:text-blue-300">Token → vector</p><h3 className="mt-1 text-lg font-semibold">Pooling은 학습 objective와 함께 하나의 checkpoint contract입니다</h3><div className="mt-5 grid gap-3 md:grid-cols-3">{methods.map(([name,op,check])=><div key={name} className="rounded-xl border border-blue-500/20 bg-blue-500/[0.05] p-4"><p className="font-semibold">{name}</p><p className="mt-3 break-words font-mono text-xs">{op}</p><p className="mt-3 text-sm leading-6 text-muted-foreground">{check}</p></div>)}</div><p className="mt-4 text-sm text-muted-foreground">token states → pooling → optional normalization → similarity metric</p></div>}
