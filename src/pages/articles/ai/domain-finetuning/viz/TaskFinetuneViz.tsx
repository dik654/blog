import VizFrame from "@/components/viz/VizFrame";
const rows=[["Input","role template·context·truncation","model이 실제로 읽는 조건"],["Target","schema·label·abstention·ambiguity","원하는 behavior의 관측 형태"],["Loss","response mask·reduction·sample weight","무엇을 정답으로 채점하는지"],["Update","full·LoRA·head-only","바뀔 parameter와 state"],["Evaluation","domain·time·general·format","성능과 회귀의 경계"]];
export default function TaskFinetuneViz(){return <VizFrame eyebrow="Task contract" title="Demonstration 파일만 저장하지 않고 다섯 경계를 함께 versioning합니다"><div className="border-y border-border">{rows.map(([name,fields,meaning])=><div key={name} className="grid gap-2 border-b border-border py-5 last:border-b-0 sm:grid-cols-[7rem_minmax(0,1.25fr)_minmax(0,1fr)] sm:gap-6"><strong>{name}</strong><span>{fields}</span><span className="text-muted-foreground">{meaning}</span></div>)}</div></VizFrame>}
