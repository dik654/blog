import VizFrame from "@/components/viz/VizFrame";
const rows=[["2D + pool","frame encoder → reducer","시간 순서 없는 기준선"],["I3D","space–time kernel","2D weight inflation + video pretraining"],["R(2+1)D","spatial conv → activation → temporal conv","factorized optimization path"],["SlowFast","low-rate wide + high-rate narrow","두 temporal resolution 결합"]];
export default function CNN3DViz(){return <VizFrame eyebrow="Temporal operator" title="시간 관계를 넣는 위치와 비용을 같은 frame budget에서 비교합니다"><div className="border-y border-border">{rows.map(([name,path,note])=><div key={name} className="grid gap-2 border-b border-border py-5 text-sm last:border-b-0 sm:grid-cols-[7rem_minmax(0,1fr)_minmax(0,1fr)] sm:gap-6"><strong>{name}</strong><code className="break-words text-xs text-orange-800 dark:text-orange-200">{path}</code><span className="text-muted-foreground">{note}</span></div>)}</div></VizFrame>}
