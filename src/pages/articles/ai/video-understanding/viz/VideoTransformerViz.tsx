import VizFrame from "@/components/viz/VizFrame";
const rows=[["Joint","all TS tokens","(TS)² pair proxy"],["Divided","temporal then spatial","ST² + TS²"],["Factorized encoder","per-frame spatial → clip temporal","early spatial compression"],["VideoMAE","visible tubelets → decoder","masking changes pretraining input"]];
export default function VideoTransformerViz(){return <VizFrame eyebrow="Video-token budget" title="Tubelet을 만든 뒤 어느 축에서 interaction을 제한하는지 비교합니다"><div className="border-y border-border">{rows.map(([name,path,cost])=><div key={name} className="grid gap-2 border-b border-border py-5 text-sm last:border-b-0 sm:grid-cols-[8rem_minmax(0,1fr)_minmax(0,1fr)] sm:gap-6"><strong>{name}</strong><span className="text-muted-foreground">{path}</span><code className="break-words text-xs text-fuchsia-800 dark:text-fuchsia-200">{cost}</code></div>)}</div><p className="mt-6 border-l border-fuchsia-500 pl-4 text-sm leading-6 text-muted-foreground">Accuracy 표에는 T·H·W·tubelet·test clip 수를 함께 적어 architecture와 input budget 효과를 분리합니다.</p></VizFrame>}
