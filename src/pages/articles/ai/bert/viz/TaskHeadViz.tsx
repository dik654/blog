const tasks=[["Sequence","h[CLS]","class logits"],["Token","h₁…hₙ","NER/POS logits"],["Span","h₁…hₙ","start + end logits"],["Retrieval","pooled h","embedding 또는 pair score"]] as const;
export default function TaskHeadViz(){return <figure data-viz="bert-heads" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">Task가 요구하는 출력 단위가 읽을 hidden state를 정한다</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-4 sm:p-6">{tasks.map(([name,reads,out],i)=><div key={name} className="rounded-lg border border-border/70 bg-background p-4"><p className="font-mono text-xs font-black text-primary">0{i+1}</p><p className="mt-2 text-sm font-bold">{name}</p><p className="mt-3 font-mono text-xs">{reads}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{out}</p></div>)}</div></figure>}
