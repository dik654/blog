const rows=[["Quantization","number representation","weight / activation bytes","codebook · kernel"],["Pruning","connections / shape","indices or smaller graph","pattern · compiler"],["Distillation","student behavior","smaller architecture","teacher signal · training"],["LoRA","adaptation update","trainable state","merge / adapter serving"]] as const;
export default function SynergyViz(){return <figure data-viz className="overflow-hidden rounded-xl border border-border bg-card"><figcaption className="border-b border-border px-4 py-4 sm:px-5"><p className="text-xs font-semibold text-muted-foreground">Compression lever boundary</p><h3 className="mt-1 text-base font-semibold">기법 이름보다 바뀌는 artifact와 실행 전제를 봅니다</h3></figcaption><div className="overflow-x-auto"><div className="min-w-[700px]"><div className="grid grid-cols-[.9fr_1.25fr_1.25fr_1.25fr] border-b border-border bg-muted/25 px-5 py-2.5 text-xs font-medium text-muted-foreground"><span>Lever</span><span>바꾸는 대상</span><span>직접 줄이는 항</span><span>필요 전제</span></div>{rows.map(row=><div key={row[0]} className="grid grid-cols-[.9fr_1.25fr_1.25fr_1.25fr] border-b border-border/70 px-5 py-3 text-sm last:border-b-0"><strong>{row[0]}</strong>{row.slice(1).map((v,i)=><span key={i} className="pr-3 text-muted-foreground">{v}</span>)}</div>)}</div></div><p className="border-t border-border bg-muted/20 px-4 py-3 text-xs leading-5 text-muted-foreground sm:px-5">Serving bottleneck과 직접 연결되지 않는 lever는 후보에서 먼저 제외합니다.</p></figure>}
