export const STEPS = [
  {
    label: '단일 뉴런 = 직선 방정식',
    body: '뉴런 하나의 연산: z = w·x + b → a = f(z)\nw = 기울기(slope): 입력 x가 출력에 미치는 영향도\nb = 절편(intercept): x=0일 때 기본 출력값\n예: w=0.5, x=2.0, b=0.1 → z=1.1 → a=σ(1.1)≈0.75\n활성화 함수 f(예: ReLU, sigmoid)가 비선형성을 추가하여\n선형 조합만으로는 표현 불가능한 곡선 결정 경계를 학습.',
  },
  {
    label: '다층 신경망 — 순차적 변환',
    body: '각 층의 연산 패턴: z⁽ˡ⁾ = W⁽ˡ⁾·a⁽ˡ⁻¹⁾ + b⁽ˡ⁾ → a⁽ˡ⁾ = f(z⁽ˡ⁾)\n입력 → [선형변환 → 활성화] × L번 반복 → 출력\n은닉층: f = ReLU(max(0,z)) 또는 GELU — 비선형성 추가\n출력층: 분류 → softmax(확률 합=1), 회귀 → 항등함수(z 그대로)\n왜 깊게 쌓는가? 얕은 네트워크로도 이론상 가능하지만,\n깊은 네트워크가 지수적으로 적은 파라미터로 같은 함수를 표현.',
  },
  {
    label: '행렬 크기 추적 — MNIST 예시',
    body: 'MNIST 2층 MLP 차원 흐름:\nx ∈ R⁷⁸⁴ (28×28 이미지 평탄화)\n→ W⁽¹⁾(128×784)·x + b⁽¹⁾(128) → a⁽¹⁾ ∈ R¹²⁸ (은닉 표현)\n→ W⁽²⁾(10×128)·a⁽¹⁾ + b⁽²⁾(10) → ŷ ∈ R¹⁰ (10개 숫자 확률)\n파라미터 합산: W⁽¹⁾=100,352 + b⁽¹⁾=128 + W⁽²⁾=1,280 + b⁽²⁾=10 = 총 101,770개\n차원 규칙: W의 행 = 출력 뉴런 수, 열 = 입력 뉴런 수.',
  },
  {
    label: '배치 처리 — 행렬곱 한 줄',
    body: '배치(batch): B개 입력을 행렬로 묶어 동시 계산\nX(B×d) @ W.T(d×h) + b = Z(B×h)\n예: B=32, d=784, h=128일 때\nX(32×784) @ W.T(784×128) + b = Z(32×128) → relu → A(32×128)\n32개 이미지의 128차원 은닉 표현이 행렬곱 한 줄로 완성.\n왜 배치? ① GPU 병렬화 극대화 ② gradient 추정 노이즈 감소\n③ 메모리 효율 (전체 데이터보다 작고, 단일보다 안정적).',
  },
  {
    label: 'GPU 벡터화와 메모리',
    body: 'CPU(NumPy)와 GPU(PyTorch) 코드 문법 동일:\nZ = X @ W.T + b  # 두 환경 모두 같은 한 줄\n차이는 속도: GPU의 수천 CUDA 코어가 각 행렬 원소를 병렬 계산 → 1000배+ 빠름.\nForward 중간값(a⁽¹⁾, a⁽²⁾, ...) 저장 필수: backward에서 ∂L/∂W 계산에 재사용.\n이 중간값 저장이 GPU 메모리 병목의 주원인 — batch 크기를 줄이거나\ngradient checkpointing(중간값 일부만 저장 후 재계산)으로 완화.',
  },
];

export const C = {
  input: '#3b82f6',
  weight: '#10b981',
  act: '#f59e0b',
  output: '#ef4444',
  batch: '#8b5cf6',
  dim: '#94a3b8',
};
