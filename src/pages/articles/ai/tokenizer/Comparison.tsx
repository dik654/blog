import ExplainedFormula from "@/components/ui/explained-formula";
import TokenizerCompareViz from "./viz/TokenizerCompareViz";
export default function Comparison(){return <section id="comparison" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">한국어 tokenizer는 algorithm 이름이 아니라 traffic으로 평가한다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="leading-7">한국어는 조사·어미가 결합하고 NFC 한 글자가 UTF-8에서 대개 여러 byte를 쓰므로 영어 중심 corpus로 학습한 tokenizer에서 sequence가 길어질 수 있다. 그러나 BPE·WordPiece·Unigram 중 하나가 항상 우월한 것은 아니다. Training corpus의 언어 비율, vocabulary budget, normalization, pre-tokenizer와 byte fallback이 실제 길이를 함께 결정한다.</p><p>서비스 traffic에서 한국어·영어·code·숫자·URL·emoji·오타를 층화 sampling하고 같은 special-token·truncation 규칙으로 encode한다. 전체 평균만 보면 소수 언어와 긴 tail document의 손실이 가려지므로 domain별 p50/p95, fallback, 잘린 비율과 round-trip을 따로 본다.</p></div><TokenizerCompareViz/><ExplainedFormula question="Vocabulary를 키워 sequence를 줄일 때 model parameter는 얼마나 늘어날까?" idea={<>Token ID마다 d차원 embedding row가 필요합니다. Input embedding과 output head weight를 공유하면 Vd, 공유하지 않으면 두 matrix가 필요해 약 2Vd parameter가 듭니다.</>} formula={String.raw`P_{\mathrm{vocab}}=\begin{cases}Vd&\text{tied}\\2Vd&\text{untied}\end{cases}`}
annotatedFormula={String.raw`P_{\mathrm{vocab}}=\begin{cases}\underbrace{V}_{\text{vocabulary size 계산}}d&\text{tied}\\2Vd&\text{untied}\end{cases}`}
operations={[
  { expression: String.raw`V`, annotation: ["vocabulary size이(가) 식의 결과에 기여하는","방식을 계산합니다.","Token ID마다"] },
]} terms={[{symbol:"V",name:"vocabulary size",description:"Regular token과 reserved special token을 포함하는 ID 공간 크기입니다."},{symbol:"d",name:"model width",description:"각 token embedding과 output logit projection의 hidden dimension입니다."}]} assumptions={["Bias, adaptive/ factorized embedding과 quantization은 제외한 단순 parameter count입니다.","Tokenizer를 바꾸어도 기존 embedding 의미가 자동으로 이전되지 않습니다."]} interpretation="큰 vocabulary는 흔한 문자열을 한 token으로 묶어 sequence와 attention 비용을 줄일 수 있지만 embedding memory와 rare row의 학습 data 요구를 늘립니다. 따라서 token 수 최소화만으로 vocabulary size를 정하지 않습니다."/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>과금·context·품질은 같은 지표가 아니다</h3><p>Token 단위 API에서는 encoded length가 비용과 context 사용량에 직접 연결되지만 provider와 model마다 encoding이 다르다. 자체 model에서는 짧은 sequence가 throughput에 유리해도 segmentation이 downstream 품질·copy fidelity·숫자와 code 처리에 미치는 영향은 별도 pretraining 또는 task evaluation이 필요하다.</p><h3>배포 artifact를 하나의 compatibility bundle로 관리한다</h3><p>Tokenizer model/vocab, merge table, normalizer, pre-tokenizer, special-token string과 ID, BOS/EOS 삽입 규칙, padding side와 model checkpoint hash를 함께 versioning한다. Golden corpus에 대해 token string·ID·offset·decoded text를 snapshot test로 남기면 library upgrade 때 조용히 입력 의미가 바뀌는 일을 잡을 수 있다.</p></div></section>}
