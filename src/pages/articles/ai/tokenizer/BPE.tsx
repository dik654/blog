import ExplainedFormula from "@/components/ui/explained-formula";
import BPEMergeViz from "./viz/BPEMergeViz";
export default function BPE(){return <section id="bpe" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">BPE는 corpus 빈도를 merge rank로 바꾼다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="leading-7">Subword BPE는 초기 symbol sequence에서 인접 pair 빈도를 세고 가장 자주 붙는 pair를 새 symbol로 병합한다. 이 과정을 반복한 결과는 vocabulary와 순서가 있는 merge table이다. Encoding에서는 corpus 빈도를 다시 세지 않고 해당 table의 우선순위로 입력 symbol을 합친다.</p></div><BPEMergeViz/><ExplainedFormula question="BPE training의 한 round에서 어떤 pair를 새 token으로 만들까?" idea={<>현재 corpus segmentation에서 모든 인접 symbol pair의 발생 횟수를 센 뒤 가장 많은 pair를 선택합니다. 병합하면 corpus를 다시 표현하므로 다음 round의 count도 바뀝니다.</>} formula={String.raw`(a^*,b^*)=\arg\max_{(a,b)\in\mathcal P}\operatorname{count}_{\mathcal C}(a,b)`} terms={[{symbol:String.raw`\mathcal C`,name:"segmented corpus",description:"현재까지 학습한 merge가 적용된 전체 training corpus입니다."},{symbol:String.raw`\mathcal P`,name:"adjacent pairs",description:"현재 segmentation에서 실제로 이웃한 symbol pair 집합입니다."},{symbol:"(a^*,b^*)",name:"next merge",description:"새 vocabulary symbol ab가 되는 pair입니다."}]} assumptions={["같은 빈도의 tie-break, word boundary와 pre-tokenizer 규칙을 구현에 고정합니다.","Vocabulary budget에 도달할 때까지 greedy하게 반복하므로 전체 최적 compression을 보장하지 않습니다."]} interpretation="BPE는 merge 당시의 local frequency를 보존합니다. Token의 언어학적 형태소성이나 downstream utility를 직접 최적화하는 식은 아니므로 corpus와 pre-tokenizer가 바뀌면 merge 결과도 달라집니다."/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>Byte-level BPE는 초기 alphabet을 byte로 둔 구현 계열이다</h3><p>UTF-8 byte 256개를 표현할 수 있으면 training vocabulary에 없던 문자열도 byte sequence로 내릴 수 있어 character-level <code>UNK</code>를 피하기 쉽다. 그러나 한글 한 글자는 여러 byte이고 드문 script는 여러 token으로 남을 수 있다. OpenAI의 <a href="https://github.com/openai/tiktoken" target="_blank" rel="noreferrer">tiktoken</a>처럼 reversible byte-level BPE도 있지만, 모든 BPE 구현이 byte 기반이거나 무조건 lossless인 것은 아니다. Normalizer, regex pre-tokenizer, byte mapping과 special-token policy를 함께 확인한다.</p></div><div id="paper-bpe" className="not-prose my-8 border-l border-primary/50 pl-4 scroll-mt-24"><p className="text-xs font-bold text-primary">논문 읽기 · Subword BPE</p><p className="mt-2 text-sm font-semibold">Neural Machine Translation of Rare Words with Subword Units</p><p className="mt-2 text-sm leading-6 text-muted-foreground">희귀 단어와 open vocabulary 번역을 위해 character sequence에 BPE를 적용해 frequent character sequence를 subword로 학습했습니다. 모든 현대 byte-level tokenizer의 normalization·regex·special-token 규격을 정의한 논문은 아닙니다.</p><a className="mt-3 inline-block text-sm font-medium text-primary underline-offset-4 hover:underline" href="https://arxiv.org/abs/1508.07909" target="_blank" rel="noreferrer">원 논문과 학습 절차 보기</a></div></section>}
