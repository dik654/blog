const paths=[["A","▁token · izer","0.42 × 0.18"],["B","▁to · ken · iz · er","0.31 × 0.12 × 0.09 × 0.08"],["C","▁tokenizer","0.006"]] as const;
export default function UnigramViz(){return <figure data-viz="unigram-paths" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">Unigram은 가능한 segmentation path의 확률을 비교한다</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-3 sm:p-6">{paths.map(([id,path,score],i)=><div key={id} className={`rounded-lg border p-4 ${i===0?"border-primary/40 bg-primary/[0.05]":"border-border/70 bg-background"}`}><p className="font-mono text-xs font-black text-primary">PATH {id}</p><p className="mt-3 break-words font-mono text-xs leading-5">{path}</p><p className="mt-2 text-xs text-muted-foreground">{score}</p></div>)}</div><p className="border-t border-border/60 px-4 py-3 text-xs leading-5 text-muted-foreground sm:px-6">수치는 원리 설명용이며 실제 token probability는 학습 corpus에서 추정합니다.</p></figure>}
