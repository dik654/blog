const rounds=[["Alphabet","l · o · w · e · r"],["rank 0","e r → er"],["rank 1","l o → lo"],["rank 2","lo w → low"]] as const;
export default function BPEMergeViz(){return <figure data-viz="bpe-merge" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">Training은 merge rank를 만들고 encoding은 그 rank를 적용한다</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-4 sm:p-6">{rounds.map(([title,value],i)=><div key={title} className={`rounded-lg border p-4 ${i===3?"border-primary/40 bg-primary/[0.05]":"border-border/70 bg-background"}`}><p className="font-mono text-xs font-bold text-primary">{title}</p><p className="mt-3 break-words font-mono text-sm">{value}</p></div>)}</div><p className="border-t border-border/60 px-4 py-3 text-xs leading-5 text-muted-foreground sm:px-6">실제 결과는 초기 alphabet, pre-tokenizer와 merge table 전체에 의존합니다.</p></figure>}
