const rows=[["사람이 보는 문자","각"],["Unicode code point","U+AC01"],["NFD code points","U+1100 U+1161 U+11A8"],["UTF-8 bytes","EA B0 81"]] as const;
export default function TextContractViz(){return <figure data-viz="text-contract" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">한 글자·code point·byte는 같은 단위가 아니다</p></figcaption><div className="divide-y divide-border/60">{rows.map(([name,value])=><div key={name} className="grid grid-cols-[minmax(0,1fr)_minmax(0,1.3fr)] gap-4 px-4 py-3 text-xs leading-5 sm:px-6"><b>{name}</b><span className="min-w-0 break-words font-mono text-muted-foreground">{value}</span></div>)}</div></figure>}
