import HookPointViz from "./viz/HookPointViz";
export default function ResidualStream(){return <section id="residual-stream" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">해석보다 먼저 measurement target을 고정한다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="leading-7">Transformer의 residual stream은 각 block이 읽고 attention·MLP 결과를 더하는 공통 hidden space다. 그러나 <code>resid_pre</code>, attention output, MLP output과 <code>resid_post</code>는 서로 다른 계산을 담는다. SAE가 어디에 학습됐는지 빠지면 “12층 feature”라는 표현만으로 결과를 재현할 수 없다.</p><p>
            Activation sample의 단위도 layer 전체가 아니라 보통 token position 하나의 vector다. 기록에는 model checkpoint,
            tokenizer, layer와 hook, token 위치, corpus sampling, context length와 activation normalization이 모두
            들어가야 한다. Gemma Scope가 layer·sublayer별로 별도 SAE를 공개한 이유도 하나의 dictionary가 모든 위치를 대표하지 않기 때문이다.
          </p></div><HookPointViz/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>Decoder direction은 그 SAE 안에서만 좌표가 된다</h3><p>Hidden dimension이 d라면 관측 activation은 d차원이지만 dictionary width m은 보통 d보다 크다. 이 overcomplete dictionary의 decoder column 하나를 feature direction이라 부른다. 같은 feature index라도 다른 checkpoint나 seed에서는 column이 재배열·분할될 수 있으므로 번호만으로 feature를 비교하지 않는다.</p></div></section>}
