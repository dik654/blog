import FailureModesViz from "./viz/FailureModesViz";
export default function Limitations(){return <section id="limitations" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">SAE는 유용한 microscope지만 완성된 모델 설명서는 아니다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="leading-7">해석 가능한 사례 몇 개와 높은 FVE만으로 SAE 품질을 확정할 수 없다. Reconstruction residual에는 작은 크기지만 behavior에 중요한 성분이 남을 수 있고, dictionary width·seed·training corpus를 바꾸면 feature가 갈라지거나 합쳐진다. 특정 feature를 찾지 못한 것이 LLM에 그 개념이 없다는 뜻인지 SAE가 놓쳤다는 뜻인지도 구분하기 어렵다.</p></div><FailureModesViz/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>Claim을 model·hook·dataset·intervention 범위로 제한한다</h3><p>“모델이 기만을 생각했다”보다 “이 model checkpoint의 특정 hook에서 SAE feature j가 이 prompt set에 활성화됐고, 정해진 α의 intervention에서 rubric score가 변했다”라고 기록하는 편이 검증 가능하다. SAE checkpoint, feature index, activation threshold, prompt sampling, control direction, effect size와 부작용을 함께 남긴다.</p><h3>Feature에서 circuit으로 가려면 연결 증거가 더 필요하다</h3><p>Crosscoder와 transcoder는 여러 layer 또는 block 입출력 사이의 feature를 연결하려 하지만 ground-truth circuit을 자동으로 제공하지 않는다. SAE로 candidate를 찾고 activation patching·attribution·targeted task evaluation으로 계산 경로를 좁히는 순서가 안전하다. 최종 결론은 reconstruction, interpretability와 causal behavior라는 서로 다른 축에서 각각 통과해야 한다.</p></div></section>}
