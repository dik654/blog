const metrics=[["FVE","복원한 activation variance"],["L0","sample당 active latent 수"],["Δ loss","복원 후 language-model loss"],["Dead","한 번도 켜지지 않는 latent"]] as const;
export default function QualityFrontierViz(){return <figure data-viz="sae-quality" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">좋은 SAE는 한 숫자가 아니라 frontier 위에 있다</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Dictionary width가 다른 checkpoint는 같은 sparsity 또는 같은 recovery 조건에서 비교합니다.</p></figcaption><div className="grid grid-cols-2 gap-3 p-4 sm:grid-cols-4 sm:p-6">{metrics.map(([name,body])=><div key={name} className="rounded-lg border border-border/70 bg-background p-4"><p className="font-mono text-sm font-black text-primary">{name}</p><p className="mt-2 text-xs leading-5 text-muted-foreground">{body}</p></div>)}</div></figure>}
