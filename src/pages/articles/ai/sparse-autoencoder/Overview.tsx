import { Link } from "react-router-dom";
import InterpretationContractViz from "./viz/InterpretationContractViz";
export default function Overview(){return <section id="overview" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">SAE는 activation에 새 좌표를 붙이는 사후 분석 모델이다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="text-lg leading-8">LLM 내부의 neuron 하나가 사람이 아는 개념 하나와 정확히 대응한다면 해석은 쉬울 것이다. 실제 activation은 여러 계산이 겹친 dense vector이며 neuron 축에서 바로 이름을 붙이기 어렵다. Sparse autoencoder(SAE)는 이미 학습된 LLM을 고정한 채 activation 표본을 모으고, 이를 더 큰 dictionary의 소수 feature direction으로 복원한다.</p><p>중요한 경계가 있다. SAE feature는 LLM에 원래 등록돼 있던 개념 ID가 아니라 특정 corpus·hook point·SAE objective에서 학습된 좌표다. Activation 사례가 일관돼 보이는 것은 해석 가설의 출발점이며, 모델이 실제 계산에 그 feature를 사용한다는 결론은 intervention이 필요하다. 일반 <Link to="/ai/autoencoder">autoencoder의 reconstruction 원리</Link>는 연결 글에 맡기고 여기서는 overcomplete sparsity와 mechanistic interpretability의 증거 계약을 다룬다.</p></div><InterpretationContractViz/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>Mechanistic interpretability 전체를 SAE 하나로 대체하지 않는다</h3><p>SAE는 후보 representation을 좁혀 주지만 그 feature를 어떤 attention head와 MLP가 계산하고 다음 layer가 어디에 사용하는지 자동으로 보여 주지는 않는다. Attribution·activation patching·circuit 분석과 독립 evaluation을 연결해야 계산 경로에 관한 claim으로 확장할 수 있다.</p></div></section>}
