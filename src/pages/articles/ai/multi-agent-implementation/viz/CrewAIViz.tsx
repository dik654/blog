const flow=[["validate_event","code","schema·identity"],["research_crew","Crew","sources[]·claims[]"],["validate_output","code","citation·policy"],["persist_or_escalate","Flow","state transition"]] as const;
export default function CrewAIViz(){return <figure data-viz className="rounded-xl border border-border bg-background p-4 sm:p-5"><figcaption><p className="text-xs font-semibold uppercase tracking-[0.14em] text-primary">Crew inside a Flow</p><p className="mt-1 font-semibold">판단 구간만 Crew에 맡기고 transition은 Flow가 강제합니다</p></figcaption><div className="mt-5 divide-y divide-border border-y border-border">{flow.map(([node,owner,output],i)=><div key={node} className="grid gap-2 py-3 sm:grid-cols-[3rem_10rem_5rem_1fr] sm:items-center"><span className="text-xs font-semibold text-muted-foreground">0{i+1}</span><span className="font-mono text-sm">{node}</span><span className={`text-xs font-semibold ${owner==="Crew"?"text-primary":"text-muted-foreground"}`}>{owner}</span><span className="text-xs text-muted-foreground">output: {output}</span></div>)}</div><p className="mt-4 text-xs leading-5 text-muted-foreground">Retry·timeout·human approval·compensation은 Crew의 자유로운 대화가 아니라 명시적 state transition으로 테스트합니다.</p></figure>}
