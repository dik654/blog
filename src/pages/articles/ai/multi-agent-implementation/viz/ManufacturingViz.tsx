export default function ManufacturingViz(){return <figure data-viz className="rounded-xl border border-border bg-background p-4 sm:p-5"><figcaption><p className="text-xs font-semibold uppercase tracking-[0.14em] text-primary">Advisory path ≠ control path</p><p className="mt-1 font-semibold">분석 결과는 안전 경계 밖에서 바로 actuator command가 되지 않습니다</p></figcaption><div className="mt-5 grid gap-6 lg:grid-cols-2"><div><p className="text-xs font-semibold text-muted-foreground">판단·보고 경로</p><div className="mt-3 divide-y divide-border border-y border-border">{["센서 정합·통계 code","매뉴얼·정비 이력 retrieval","LLM report·evidence validator"].map((x,i)=><div key={x} className="grid grid-cols-[2rem_1fr] gap-2 py-3 text-sm"><span className="text-xs text-muted-foreground">0{i+1}</span><span>{x}</span></div>)}</div><p className="mt-3 text-xs text-muted-foreground">출력: advisory report · uncertainty · source trace</p></div><div><p className="text-xs font-semibold text-primary">실행·안전 경로</p><div className="mt-3 divide-y divide-border border-y border-border">{["Sensor validity","Approved deterministic rule","Authorized operator approval","PLC / physical interlock"].map(x=><div key={x} className="py-3 text-sm">□ {x}</div>)}</div><p className="mt-3 text-xs text-muted-foreground">네 조건이 모두 참일 때만 versioned command를 실행합니다.</p></div></div><div className="mt-5 border-l-2 border-primary pl-4 text-sm leading-6"><span className="font-semibold">권한 경계</span><span className="text-muted-foreground"> · agent credential에는 PLC write 없음 · fail-safe와 emergency stop은 agent runtime 밖에서 유지</span></div></figure>}
