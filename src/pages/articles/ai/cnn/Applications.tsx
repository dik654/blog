import { Link } from "react-router-dom";
import TaskHeadViz from "./viz/TaskHeadViz";
export default function Applications(){return <section id="applications" className="mb-16 scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">Task가 바뀌면 보존해야 할 spatial information이 달라진다</h2><div className="prose prose-neutral dark:prose-invert max-w-none"><p className="leading-7">Classification은 image 전체를 하나의 representation으로 집계해도 되지만 detection과 segmentation은 object·pixel 위치를 출력해야 한다. Early downsampling으로 spatial detail을 지우면 head가 복원할 수 없으므로 feature pyramid, skip connection, dilation과 upsampling 경로가 필요하다. Restoration은 output image를 만들기 때문에 local texture와 global structure를 모두 보존해야 한다.</p></div><TaskHeadViz/><div className="prose prose-neutral dark:prose-invert max-w-none"><h3>Transfer learning은 freeze 여부보다 distribution과 normalization 계약을 본다</h3><p>Pretrained backbone에 새 head만 학습하는 것은 빠른 baseline이지만 source와 target domain 차이가 크면 일부 또는 전체 layer를 fine-tuning하는 편이 낫다. BatchNorm running statistics를 freeze할지 갱신할지, input resolution·normalization·augmentation이 pretrained recipe와 맞는지까지 함께 검증한다.</p><p>Data split·augmentation·fine-tuning 실험 순서는 <Link to="/ai/image-classification-pipeline">Image classification pipeline 글</Link>에서 이어진다.</p></div></section>}
