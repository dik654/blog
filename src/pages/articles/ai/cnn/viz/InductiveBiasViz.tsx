const ideas=[["Locality","작은 neighborhood부터 읽음"],["Weight sharing","같은 kernel을 모든 위치에 적용"],["Hierarchy","layer를 지나 receptive field 확장"]] as const;
export default function InductiveBiasViz(){return <figure data-viz="cnn-bias" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">CNN이 image grid에 미리 넣는 세 가지 가정</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-3 sm:p-6">{ideas.map(([title,body],i)=><div key={title} className="rounded-lg border border-border/70 bg-background p-4"><p className="font-mono text-xs font-black text-primary">0{i+1}</p><p className="mt-2 text-sm font-bold">{title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{body}</p></div>)}</div><p className="border-t border-border/60 px-4 py-3 text-xs leading-5 text-muted-foreground sm:px-6">가정이 data와 맞으면 sample efficiency를 얻고, 맞지 않으면 model flexibility의 제약이 됩니다.</p></figure>}
