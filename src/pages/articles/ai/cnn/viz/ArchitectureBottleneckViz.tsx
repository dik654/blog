const rows=[["ResNet","optimization path","identity shortcut"],["MobileNet","channel mixing cost","depthwise + pointwise"],["ConvNeXt","현대 block/training","large kernel·stage design"],["Dilated CNN","dense context","resolution 유지 + dilation"]] as const;
export default function ArchitectureBottleneckViz(){return <figure data-viz="cnn-milestones" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">Architecture 이름보다 없앤 bottleneck을 읽는다</p></figcaption><div className="divide-y divide-border/60">{rows.map(([name,problem,mechanism],i)=><div key={name} className="grid grid-cols-[5.5rem_minmax(0,1fr)] gap-3 px-4 py-3 text-xs leading-5 sm:grid-cols-[6rem_9rem_minmax(0,1fr)] sm:px-6"><b className="text-primary">{name}</b><span>{problem}</span><span className="col-start-2 text-muted-foreground sm:col-start-auto">{mechanism}</span></div>)}</div></figure>}
