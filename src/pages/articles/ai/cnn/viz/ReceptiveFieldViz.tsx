const levels=[["Layer 1","3×3","local edge"],["Layer 2","5×5","edge combination"],["Layer 3","7×7","larger pattern"]] as const;
export default function ReceptiveFieldViz(){return <figure data-viz="receptive-field" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">작은 kernel도 layer를 지나 input 좌표의 넓은 영역과 연결된다</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-3 sm:p-6">{levels.map(([layer,size,body],i)=><div key={layer} className={`rounded-lg border p-4 ${i===2?"border-primary/40 bg-primary/[0.05]":"border-border/70 bg-background"}`}><p className="text-xs font-bold text-primary">{layer}</p><p className="mt-2 font-mono text-xl font-black">{size}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{body}</p></div>)}</div><p className="border-t border-border/60 px-4 py-3 text-xs leading-5 text-muted-foreground sm:px-6">이론적 범위 안의 모든 pixel이 같은 영향력을 갖는 것은 아닙니다.</p></figure>}
