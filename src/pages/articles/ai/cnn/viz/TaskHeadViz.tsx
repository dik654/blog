const tasks=[["Classification","global","class logits"],["Detection","multi-scale","box + class"],["Segmentation","dense spatial","pixel logits"],["Restoration","encoder–decoder","output image"]] as const;
export default function TaskHeadViz(){return <figure data-viz="vision-heads" className="not-prose my-9 overflow-hidden rounded-xl border border-border/70 bg-card"><figcaption className="border-b border-border/60 px-4 py-4 sm:px-6"><p className="text-sm font-bold">같은 backbone도 task가 요구하는 spatial output이 다르다</p></figcaption><div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-4 sm:p-6">{tasks.map(([name,reads,out],i)=><div key={name} className="rounded-lg border border-border/70 bg-background p-4"><p className="font-mono text-xs font-black text-primary">0{i+1}</p><p className="mt-2 text-sm font-bold">{name}</p><p className="mt-3 text-xs">{reads}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{out}</p></div>)}</div></figure>}
