import type { StepDef } from '@/components/ui/step-viz';

export const STEPS: StepDef[] = [
  {
    label: 'Momentum 수식 — v_t = β·v + ∇L',
    body: '수식: v_t = β · v_(t-1) + ∇L(θ_t), 그 후 θ_(t+1) = θ_t − η · v_t\nv = 속도(velocity) 벡터 — 그래디언트의 지수이동평균(EMA). β = 감쇠 계수(통상 0.9), η = 학습률.\n물리 비유: 공이 경사면을 굴러 내려감. β·v_(t-1)은 관성(이전 속도), ∇L은 현재 기울기(중력).\n같은 방향 그래디언트가 반복되면 v가 누적 → 종단속도(terminal velocity) η/(1−β)에 수렴.\n예: β=0.9, η=0.01이면 최대 유효 LR = 0.01/(1−0.9) = 0.1 — SGD 대비 10배 가속.\n반대 방향 그래디언트는 서로 상쇄 → 좁은 골짜기의 지그재그 진동이 자연스럽게 감쇠.',
  },
  {
    label: 'β 효과 — 0이면 SGD, 0.9이면 10스텝 기억',
    body: 'EMA 윈도우 = 1/(1−β). β=0 → 윈도우 1(순수 SGD, 관성 없음). β=0.9 → 윈도우 ~10스텝(표준값). β=0.99 → ~100스텝(대규모 배치/LLM).\nt스텝 전 그래디언트의 가중치 = βᵗ. β=0.9일 때 10스텝 전 가중치 = 0.9¹⁰ ≈ 0.35, 20스텝 전 ≈ 0.12 → 최근 10스텝이 지배적.\nβ가 클수록: 관성 강화 → 부드러운 궤적, 하지만 방향 전환이 느려 오버슈팅 위험 증가.\nβ가 작을수록: 현재 그래디언트에 민감 → 빠른 반응, 하지만 진동 감쇠 효과 약화.\nβ ≥ 1이면 v_t가 매 스텝 증가 → 발산. β = 1이면 모든 과거 그래디언트를 동일 가중치로 누적하므로 에너지 무한 증가.\n실전: β=0.9가 거의 모든 태스크에서 기본값. 대규모 배치(8K+)에서 β=0.95~0.99 사용.',
  },
  {
    label: 'SGD vs Momentum vs NAG — 궤적 비교',
    body: 'SGD: θ_(t+1) = θ_t − η·∇L(θ_t). 골짜기에서 매 스텝 방향이 바뀌어 지그재그 → 느린 수렴.\nMomentum: v_t = β·v_(t-1) + ∇L(θ_t), θ_(t+1) = θ_t − η·v_t. 관성이 진동을 상쇄, 주 방향으로 가속.\nNAG(Nesterov): v_t = β·v_(t-1) + ∇L(θ_t − η·β·v_(t-1)). 핵심 차이 — "미래 위치"(θ − η·β·v)에서 그래디언트 측정.\nNAG의 이점: 오버슈팅이 예상되면 미래 위치의 그래디언트가 반대 방향 → 미리 감속. Momentum보다 수렴 약 20~30% 빠름.\n볼록 최적화 이론: Momentum은 O(1/T), NAG는 O(1/T²) 수렴 — Nesterov 가속이 최적 1차 방법임이 증명.\n실전: PyTorch SGD(momentum=0.9, nesterov=True)로 NAG 활성화. CNN 학습에서 표준 설정.',
  },
  {
    label: '장단점·사용 시점',
    body: '장점: ① 진동 감소 — 반대 방향 그래디언트가 v 안에서 상쇄. ② 평탄 구간(∇L≈0) 탈출 — 관성으로 계속 진행. ③ ill-conditioning 완화 — 짧은 축의 진동은 감쇠, 긴 축은 가속.\n메모리 비용: SGD 대비 v 벡터 1개 추가 → 파라미터 수 × 1 추가 상태. Adam(2개)보다 절약.\n단점: ① 파라미터별 적응 없음 — θ₁, θ₂의 스케일이 달라도 같은 v 메커니즘 적용. ② β 고정이므로 복잡한 지형에서 오버슈팅 발생 가능.\n왜 Adam 대신 사용하나: 이미지 분류(ResNet, VGG)에서 SGD+Momentum이 Adam보다 테스트 정확도 1~2% 우수한 사례 다수.\n표준 설정: SGD(lr=0.1, momentum=0.9, weight_decay=5e-4) + cosine annealing. ImageNet 학습의 사실상 표준.\nTransformer/NLP에서는 Adam 계열 선호 — Momentum만으로는 임베딩 층의 희소 업데이트를 효율적으로 처리 불가.',
  },
];

export const COLORS = {
  mom: '#8b5cf6',    // momentum 보라
  beta: '#f59e0b',   // β 관련 노랑
  nag: '#10b981',    // NAG 초록
  dim: '#94a3b8',    // 보조 텍스트
  sgd: '#0ea5e9',    // SGD 파랑
};
