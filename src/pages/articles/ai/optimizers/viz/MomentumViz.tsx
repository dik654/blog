import StepViz from "@/components/ui/step-viz";
const scenes=[{title:"현재 gradient만 사용",values:[1,-1,1,-1,1],result:"좌우 방향이 계속 바뀜"},{title:"β=0.9로 과거를 기억",values:[1,.1,1.09,-.019,.983],result:"번갈아 나오는 성분이 완화"},{title:"같은 부호가 지속",values:[1,1.9,2.71,3.44,4.1],result:"일관된 방향은 누적"},{title:"두 경로 비교",values:[1,.1,1.09,-.019,.983],result:"state가 gradient sequence를 filter"}] as const;
export default function MomentumViz(){return <StepViz steps={scenes.map(s=>`${s.title} — ${s.result}`)}>{step=>{const scene=scenes[step];const max=Math.max(...scene.values.map(Math.abs));return <div className="w-full max-w-3xl"><div className="grid grid-cols-5 gap-3">{scene.values.map((value,index)=><div key={index} className="min-w-0 text-center"><p className="font-mono text-[10px] tabular-nums text-muted-foreground">{value.toFixed(2)}</p><div className="relative mt-2 h-28 border-y border-border/50"><span className="absolute left-0 right-0 top-1/2 h-px bg-border"/><span className={`absolute left-1/2 w-5 -translate-x-1/2 ${value>=0?"bottom-1/2 bg-emerald-500/70":"top-1/2 bg-rose-500/70"}`} style={{height:`${Math.max(2,Math.abs(value)/max*50)}px`}}/></div><p className="mt-2 font-mono text-[9px]">t{index}</p></div>)}</div><p className="mt-5 text-sm font-semibold">{scene.result}</p></div>}}</StepViz>}
