import StepViz from "@/components/ui/step-viz";
const steps=["Adam + L2 — λθ도 adaptive state에 들어감","AdamW — task update와 shrink 분리","같은 θ에서 coordinate scale 비교"] as const;
export default function AdamWViz(){return <StepViz steps={steps}>{step=><div className="grid w-full max-w-3xl gap-7 md:grid-cols-2"><section className={`min-w-0 border-t border-rose-500/40 pt-4 ${step===1?"opacity-35":""}`}><p className="text-xs font-bold text-rose-600 dark:text-rose-300">L2 inside Adam</p><p className="mt-3 break-words font-mono text-xs leading-6">g ← ∇L + λθ<br/>m ← EMA(g)<br/>v ← EMA(g²)<br/>θ ← θ − ηm̂/√v̂</p><p className="mt-4 text-xs leading-5 text-muted-foreground">Penalty gradient도 coordinate-wise denominator에 흡수되어 direct shrink와 달라집니다.</p></section><section className={`min-w-0 border-t border-emerald-500/40 pt-4 ${step===0?"opacity-35":""}`}><p className="text-xs font-bold text-emerald-600 dark:text-emerald-300">Decoupled AdamW</p><p className="mt-3 break-words font-mono text-xs leading-6">g ← ∇L<br/>m,v ← Adam state(g)<br/>θ ← (1−ηλ)θ<br/>θ ← θ − ηm̂/√v̂</p><p className="mt-4 text-xs leading-5 text-muted-foreground">Task gradient adaptation과 weight shrinkage의 책임을 분리합니다.</p></section>{step===2&&<p className="md:col-span-2 border-l border-primary/40 pl-4 text-xs leading-5 text-muted-foreground">Task gradient scale이 달라도 AdamW의 direct shrink factor (1−ηλ)는 같은 parameter group 안에서 동일합니다.</p>}</div>}</StepViz>}
