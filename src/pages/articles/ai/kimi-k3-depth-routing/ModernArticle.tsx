import ContentBoundary from "@/components/articles/content-boundary";
import { CitationBlock } from "@/components/ui/citation-block";
import ExplainedFormula from "@/components/ui/explained-formula";
import ConceptLadderViz from "@/components/viz/ConceptLadderViz";
import { EvidenceGrid, LessonHeader, TermLesson } from "../kimi-k3-shared";
import { DepthRoutingViz } from "../kimi-k3-viz";

export default function KimiK3DepthRoutingArticle() {
  return <article id="overview" className="space-y-16">
    <section className="space-y-6">
      <LessonHeader number="00" eyebrow="표준 residual부터" title="Residual은 이전 층을 보존하는 통로이고, depth attention은 그 통로의 source를 고른다">먼저 일반 residual을 봅니다. Layer는 직전 state에 update를 더합니다. Attention Residuals는 이 입력을 embedding과 앞선 depth state의 weighted read로 바꿉니다.</LessonHeader>
      <TermLesson name="Standard residual stream" oneLine="직전 representation을 identity path로 보존하고 현재 sublayer가 만든 update를 더하는 depth 전달 경로입니다." shape="hₗ₊₁ = hₗ + Fₗ(norm(hₗ))" example="Fₗ가 0에 가까우면 다음 층은 hₗ을 거의 그대로 받습니다." boundary="Residual은 과거 모든 layer를 명시적으로 다시 고르는 mechanism이 아니라 직전 state에 누적된 결과를 전달합니다." />
      <DepthRoutingViz />
    </section>
    <section id="depth-attention" className="space-y-6">
      <LessonHeader number="01" eyebrow="Token이 아니라 layer source 선택" title="현재 layer의 pseudo-query가 이전 depth representation의 key를 비교한다">각 source에는 key와 value가 있습니다. 현재 layer가 가진 learned pseudo-query가 key score를 만들고, softmax weight로 value를 섞습니다.</LessonHeader>
      <TermLesson name="Attention Residuals · depth attention" oneLine="현재 layer의 learned query가 embedding·이전 depth source를 골라 weighted sum하고 그 결과를 layer input으로 사용하는 residual 대체 경로입니다." shape="layer query uₗ ↔ source keys kⱼ → weights aₗⱼ → Σⱼaₗⱼvⱼ" example="Weight [0.5,0.3,0.2]면 세 depth value를 50%·30%·20%로 섞습니다." boundary="Weight는 사용 비율이지 source의 인간적 의미나 causal 중요도를 자동 증명하지 않습니다." />
      <ExplainedFormula question="왜 depth source score에 softmax를 적용한 뒤 value를 합할까요?" idea="서로 다른 수의 source score를 양수이면서 합이 1인 비율로 바꾸면 현재 layer input을 안정적인 weighted combination으로 만들 수 있습니다." formula={String.raw`a_{\ell j}=\operatorname{softmax}_j(u_\ell^\top k_j),\quad x_\ell=\sum_j a_{\ell j}v_j`} annotatedFormula={String.raw`\begin{aligned}s_{\ell j}&=\underbrace{u_\ell^\top k_j}_{\substack{\text{query와 key를}\text{내적해 적합도 계산}}}\\a_{\ell j}&=\underbrace{\frac{e^{s_{\ell j}}}{\sum_r e^{s_{\ell r}}}}_{\substack{\text{score를}\text{합 1로 정규화}}}\\x_\ell&=\underbrace{\sum_j a_{\ell j}v_j}_{\substack{\text{source value를}\text{weight로 합성}}}\end{aligned}`} operations={[{expression:String.raw`u_\ell^\top k_j`,annotation:["현재 layer query와 source key를 내적해","depth source 적합도 계산"]},{expression:String.raw`e^{s_{\ell j}}/\sum_r e^{s_{\ell r}}`,annotation:["모든 score를 exponentiate·정규화해","합이 1인 선택 비율 생성"]},{expression:String.raw`\sum_j a_{\ell j}v_j`,annotation:["source value를 weight로 곱해 더해","현재 layer input 합성"]}]} terms={[{symbol:String.raw`u_\ell`,name:"Layer pseudo-query",description:"Layer ℓ가 어떤 depth source를 읽을지 정하는 learned vector입니다."},{symbol:String.raw`k_j,v_j`,name:"Depth key and value",description:"Source j를 고르는 주소와 실제 전달할 representation입니다."},{symbol:String.raw`a_{\ell j}`,name:"Depth attention weight",description:"현재 layer가 source j를 사용할 정규화 비율입니다."}]} assumptions={["Token position별 계산은 표기에서 생략했습니다.","Source key/value가 같은 hidden width에 맞춰져 있습니다."]} interpretation="[0.5,0.3,0.2]는 세 source를 모두 쓰되 첫 source를 가장 크게 반영한다는 뜻입니다. Token attention의 과거 token weight와 혼동하지 않습니다." />
    </section>
    <section id="block-bound" className="space-y-6">
      <LessonHeader number="02" eyebrow="93개 source를 모두 들고 있지 않기" title="Block AttnRes는 최대 12층의 결과를 한 source로 묶는다">Full 방식은 layer 수에 비례해 source state가 늘어납니다. K3는 93 main layer를 8개 block으로 묶고 block output과 embedding만 장기 source로 둡니다.</LessonHeader>
      <TermLesson name="Block AttnRes state bound" oneLine="Layer별 depth source를 block-level representation으로 묶어 long-lived source state를 layer 수 L 대신 block 수 N에 비례시키는 구현입니다." shape="93 layers ÷ max 12 layers/block → 8 block sources + embedding" example="Hidden width d라면 Full source order는 약 93d, block-level 장기 source는 약 9d입니다." boundary="이는 source-state 비례 비교이지 model 전체 memory가 93/9로 줄었다는 뜻이 아니며 block 내부 개별 layer 선택 자유도도 줄어듭니다." />
      <ExplainedFormula question="왜 93층을 12층 block으로 묶으면 8개 block source가 될까요?" idea="마지막 block이 12층을 다 채우지 않아도 필요하므로 floor가 아니라 ceiling을 사용합니다. 여기에 embedding source 하나를 더합니다." formula={String.raw`N=\left\lceil L/B\right\rceil,\quad M_{\rm source}\propto(N+1)d`} annotatedFormula={String.raw`\begin{aligned}N&=\underbrace{\left\lceil\frac{93}{12}\right\rceil}_{\substack{\text{남은 층도 세도록}\text{올림}}}=8\\S_{\rm block}&=\underbrace{N}_{\text{block source}}=8\\S_{\rm total}&=\underbrace{S_{\rm block}+1}_{\text{embedding 추가}}=9\\M_{\rm source}&\propto\underbrace{S_{\rm total}d}_{\text{source state 차수}}=9d\end{aligned}`} operations={[{expression:String.raw`\lceil93/12\rceil`,annotation:["layer 수를 block capacity로 나누고 올림해","partial 마지막 block 포함"]},{expression:String.raw`N+1`,annotation:["block source에 embedding을 더해","long-lived source 수 계산"]},{expression:String.raw`S\times d`,annotation:["source 개수에 hidden width를 곱해","source-state 크기의 차수 계산"]}]} terms={[{symbol:String.raw`L`,name:"Main-layer count",description:"Block으로 묶을 전체 layer 수입니다."},{symbol:String.raw`B`,name:"Maximum block depth",description:"Source 하나로 합칠 최대 layer 수입니다."},{symbol:String.raw`d`,name:"Hidden width",description:"각 source representation의 coordinate 수입니다."}]} assumptions={["Key/value projection과 optimizer·activation memory는 비교에서 제외합니다.","마지막 partial block도 source 하나를 만듭니다."]} interpretation="93d 대 9d는 depth-source storage의 order 비교입니다. Total parameter·KV cache·MoE state까지 같은 비율로 줄지 않습니다." />
    </section>
    <section id="evidence" className="space-y-6">
      <LessonHeader number="03" eyebrow="선택 자유도와 비용 경계" title="더 많은 source를 고르는 표현력과 source state 비용을 함께 기록한다">Full AttnRes는 세밀하지만 state와 통신이 큽니다. Block 방식은 cost를 제한하는 대신 block 내부 layer를 따로 고르는 자유를 포기합니다.</LessonHeader>
      <div id="paper-attention-residuals" className="scroll-mt-24"><CitationBlock source="Attention Residuals" citeKey={1} href="https://arxiv.org/abs/2603.15031"><EvidenceGrid problem="깊은 network에서 residual이 직전 누적 state에만 의존하는 depth bottleneck" contribution="Layer pseudo-query와 Full·Block depth attention 경로" assumptions="논문의 architecture·scale·block 설계·training 조건" scope="방법 식·복잡도·보고된 실험 범위" notClaim="Attention weight가 semantic causality를 증명하거나 모든 깊이에서 같은 이득을 낸다는 주장" /></CitationBlock></div>
      <ConceptLadderViz title="Depth routing 학습 순서" description="표준 residual에서 출발해 source selection과 block bound를 조립합니다." steps={[{label:"Residual",detail:"직전 state를 보존하는 통로를 확인합니다."},{label:"Source",detail:"이전 depth representation에 key/value를 붙입니다."},{label:"Select",detail:"Pseudo-query로 source를 가중합합니다."},{label:"Bound",detail:"Block으로 long-lived source 수를 제한합니다."}]} />
      <ContentBoundary article="kimi-k3-depth-routing" />
    </section>
  </article>;
}
