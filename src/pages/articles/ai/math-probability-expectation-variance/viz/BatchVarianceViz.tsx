import VizFrame from "@/components/viz/VizFrame";

const rows = [{b:1,v:"σ²",sd:"σ"},{b:4,v:"σ²/4",sd:"σ/2"},{b:16,v:"σ²/16",sd:"σ/4"},{b:64,v:"σ²/64",sd:"σ/8"}] as const;
export default function BatchVarianceViz(){return <VizFrame eyebrow="독립 평균의 효과" title="Batch size는 variance와 standard deviation을 서로 다른 비율로 줄입니다" description="Variance는 1/B, 눈에 보이는 흔들림의 scale인 standard deviation은 1/√B로 감소합니다."><div className="grid gap-px overflow-hidden rounded-lg border border-border/70 bg-border/60 sm:grid-cols-4">{rows.map(row=><div key={row.b} className="min-w-0 bg-background p-5"><p className="text-xs font-bold text-muted-foreground">BATCH</p><p className="mt-2 font-mono text-2xl font-bold">{row.b}</p><div className="mt-5 space-y-2 border-t border-border/60 pt-4 text-xs"><p className="flex justify-between gap-2"><span>variance</span><b className="font-mono">{row.v}</b></p><p className="flex justify-between gap-2"><span>std.</span><b className="font-mono">{row.sd}</b></p></div></div>)}</div></VizFrame>}
