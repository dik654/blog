import ContentBoundary from "@/components/articles/content-boundary";
import TermBreakdown from "@/components/articles/term-breakdown";
import ExplainedFormula from "@/components/ui/explained-formula";
import { EvaluationContractViz } from "./viz/ModernCompetitionViz";

export default function CompetitionEvaluationArticle(){return <div className="space-y-16">
  <section id="overview" className="scroll-mt-20"><h2 className="mb-6 text-2xl font-bold">평가 계약은 model보다 먼저 prediction 한 행의 의미를 고정합니다</h2><div className="prose prose-neutral max-w-none dark:prose-invert"><p className="text-lg leading-8">한 row가 환자 방문인지 환자인지, 어느 시각까지의 정보를 쓰는지, 무엇을 언제까지 맞히는지 모르면 같은 score도 서로 다른 문제를 측정합니다. 먼저 row·cutoff·target·metric·evaluation role을 한 장의 계약으로 씁니다.</p></div>
  <TermBreakdown title="Prediction question의 네 부분" items={[{term:"Prediction row",description:"Model output 하나와 target 하나가 대응하는 identity입니다.",example:"patient_id+visit_id가 한 row key입니다."},{term:"Prediction cutoff",description:"Feature source가 도착해 있어야 하는 마지막 시각입니다."},{term:"Target horizon",description:"Cutoff 뒤 어느 구간의 어떤 사건을 정답으로 삼는지 정합니다."},{term:"Metric contract",description:"Row loss·group reducer·weight·direction을 고정합니다.",boundary:"AUROC라는 이름만으로 unit과 tie rule이 정해지지 않습니다."}]}/><EvaluationContractViz/><ContentBoundary article="competition-workflow"/></section>
  <section id="target" className="scroll-mt-20"><h2 className="mb-5 text-2xl font-bold">Cutoff 뒤 target window를 열어 label을 만듭니다</h2><ExplainedFormula question="Prediction row의 target은 왜 cutoff와 horizon으로 정의하나요?" idea={<p>Cutoff 전까지는 input, cutoff 뒤 정한 horizon 안의 outcome은 target입니다. 두 구간을 분리해야 같은 사건이 feature와 label에 동시에 들어가지 않습니다.</p>} formula={String.raw`y_i=\mathbf1\{\exists e:\;c_i<t_e\le c_i+h_i\}`} annotatedFormula={String.raw`\begin{aligned}W_i&=\underbrace{(c_i,c_i+h_i]}_{\text{cutoff 뒤 target 관측 구간을 생성}}\\E_i&=\underbrace{\{e:t_e\in W_i\}}_{\text{그 구간의 target event만 선택}}\\y_i&=\underbrace{\mathbf1[|E_i|>0]}_{\text{event 존재를 binary label로 변환}}\end{aligned}`} operations={[{expression:String.raw`(c_i,c_i+h_i]`,annotation:["cutoff에 horizon을 더해","target window를 생성"]},{expression:String.raw`\{e:t_e\in W_i\}`,annotation:["event time을 window와 비교해","target events만 필터"]},{expression:String.raw`\mathbf1[|E_i|>0]`,annotation:["event count가 양수인지 확인해","binary label로 변환"]}]} terms={[{symbol:"c_i",name:"Prediction cutoff",description:"Row i의 input 정보 경계입니다."},{symbol:"h_i",name:"Target horizon",description:"미래 outcome을 관찰할 길이입니다."},{symbol:"y_i",name:"Target label",description:"계약에 따라 생성한 정답입니다."}]} assumptions={["Window endpoint convention과 timezone을 고정합니다.","Label available time은 별도 availability 계약으로 검사합니다."]} interpretation="10:00 cutoff와 24시간 horizon이면 다음 날 10:00까지의 event만 target입니다."/></section>
  <section id="metric" className="scroll-mt-20"><h2 className="mb-5 text-2xl font-bold">Metric은 row loss를 어떤 단위와 weight로 줄이는지까지 포함합니다</h2><div className="prose prose-neutral max-w-none dark:prose-invert"><p>
            Patient 하나에 여러 visits가 있으면 visit 평균과 patient 평균은 다릅니다. Higher-is-better인지 lower-is-better인지,
            missing row와 tie를 어떻게 처리하는지, group reducer와 sample weight를 함께 versioning합니다.
          </p></div></section>
  <section id="roles" className="scroll-mt-20"><h2 className="mb-5 text-2xl font-bold">Local은 선택하고 public은 제한된 feedback을 주며 private는 마지막 보고를 맡습니다</h2><div className="prose prose-neutral max-w-none dark:prose-invert"><p>어떤 score를 본 뒤 model·feature·split을 바꿀 수 있는지 역할을 먼저 적습니다. Final data를 본 뒤 다시 바꾸면 그 data도 selection에 사용된 것이므로 새 독립 평가가 필요합니다.</p></div></section>
</div>}
