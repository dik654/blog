export function PlonkImplementationViz(){const stages=[["circuit compiler","rows · selectors · copy map"],["prover plan","commit → challenge → quotient"],["opening receipt","evaluations · PCS proof"],["release","negative fixtures · rollback"]];return <div data-viz="plonk-implementation" className="rounded-xl border border-border bg-card p-4 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">fixed circuit to proof receipt</p><div className="mt-4 grid gap-3 md:grid-cols-4">{stages.map(([a,b],i)=><div key={a} className="min-w-0 rounded-lg border border-border bg-muted/20 p-4"><span className="text-xs font-semibold text-primary">0{i+1}</span><p className="mt-2 break-words font-semibold">{a}</p><p className="mt-1 break-words text-sm leading-6 text-muted-foreground">{b}</p></div>)}</div></div>}
