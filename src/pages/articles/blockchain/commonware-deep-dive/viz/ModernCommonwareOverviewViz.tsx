const Step=({tag,title,text}:{tag:string;title:string;text:string})=><div className="min-w-0 rounded-lg border border-border bg-card p-4"><p className="text-xs font-semibold text-primary">{tag}</p><p className="mt-1 font-semibold">{title}</p><p className="mt-2 break-words text-sm leading-6 text-muted-foreground">{text}</p></div>;

export function PrimitiveCompositionViz(){return <figure data-viz="commonware-composition" className="not-prose min-w-0 rounded-xl border border-border bg-muted/20 p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">한 요청은 프리미티브 사이에서 digest와 receipt를 넘겨받는다</figcaption><div className="grid gap-4 md:grid-cols-4"><Step tag="01 runtime" title="task·clock·storage context" text="실행 자원과 namespace를 application actor에 제공합니다."/><Step tag="02 network" title="authenticated payload" text="상대 identity와 channel quota를 확인해 payload를 전달합니다."/><Step tag="03 consensus" title="ordered digest" text="Simplex certificate가 payload digest의 순서를 확정합니다."/><Step tag="04 application" title="state·client receipt" text="Payload bytes를 복구·실행하고 state root와 결과를 저장합니다."/></div></figure>}

export function ReleaseParityViz(){return <figure data-viz="commonware-release" className="not-prose min-w-0 rounded-xl border border-border bg-card p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">같은 seed·event trace의 결과가 production adapter와 맞을 때만 release한다</figcaption><div className="grid gap-4 sm:grid-cols-3"><Step tag="fixture" title="fault schedule" text="Drop·delay·duplicate·crash 지점을 seed와 함께 고정합니다."/><Step tag="replay" title="deterministic digest" text="동일 입력을 반복해 message·state·receipt digest를 비교합니다."/><Step tag="gate" title="adopt or rollback" text="Parity·recovery·resource budget 중 하나라도 깨지면 이전 artifact를 유지합니다."/></div></figure>}
