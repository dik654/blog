import ExplainedFormula from "@/components/ui/explained-formula";
import { CitationBlock } from "@/components/ui/citation-block";
import { CommonwareChannelViz, CommonwareHandshakeViz } from "./viz/ModernCommonwareP2PViz";

export default function ModernCommonwareCryptoP2PArticle() { return <article className="space-y-14">
  <section id="overview" className="space-y-6"><header className="space-y-3"><p className="text-sm font-semibold text-primary">Commonware monorepo v2026.7.0</p><h2 className="text-3xl font-bold tracking-tight">Commonware P2P는 known peer를 인증한 뒤 channel별 bounded message 경로를 연다</h2></header><p className="text-lg leading-8">Alice가 Bob에게 <code>payroll-report-v1</code>의 range blocks를 보낸다고 하겠습니다. 양쪽은 미리 알고 있는 public identity와 timestamp 범위를 handshake context에 넣고 Syn→SynAck→Ack 세 message로 ephemeral shared secret과 key confirmation을 만듭니다. 그 뒤 channel ID, message size·rate와 bounded backlog를 통과한 bytes만 application receiver에 전달합니다.</p><p>Diffie–Hellman, authenticated transcript와 directional KDF의 일반 원리는 <a className="text-primary hover:underline" href="/crypto/diffie-hellman#authenticated-transcript">DH 정본</a>을 재사용합니다. 이 route는 v2026.7.0 handshake message/state, authenticated P2P channel quota와 unreliable priority mailbox의 local admission을 다룹니다. BLS·threshold certificate는 별도 crypto/consensus 정본의 책임입니다.</p><CommonwareHandshakeViz/></section>
  <section id="handshake" className="space-y-6"><header><p className="text-sm font-semibold text-primary">01 · authenticated session</p><h2 className="mt-2 text-2xl font-bold">Signature·timestamp·confirmation이 같은 transcript와 두 방향 cipher를 묶는다</h2></header><p>Dialer의 Syn에는 current time, ephemeral public key와 transcript signature가 들어갑니다. Listener는 허용 timestamp 범위와 expected identity를 검증한 뒤 자기 ephemeral key, signature와 confirmation tag를 SynAck으로 보내고, dialer Ack confirmation까지 맞아야 session을 release합니다. Shared secret에서는 send와 receive용 ChaCha20-Poly1305 cipher를 분리합니다.</p><p>각 방향 cipher는 12-byte counter를 nonce로 사용하고 성공한 send/receive마다 증가시켜 순서가 바뀐 ciphertext를 정상 message로 받아들이지 않습니다. Counter를 snapshot에서 되감아 같은 key로 재사용하거나 identity·role·namespace가 다른 transcript를 섞으면 안 됩니다.</p><ExplainedFormula question="Handshake timestamp가 replay·큰 clock skew를 막는 허용 구간 안에 있는지 어떻게 판단할까?" idea={<>Runtime이 계산한 half-open range를 handshake Context에 넣고 받은 timestamp가 그 안에 있을 때만 transcript verification을 계속합니다. 오래됐거나 지나치게 미래인 값은 같은 signature여도 거절합니다.</>} formula={String.raw`t_{peer}\in[t_{min},t_{max})`} terms={[{symbol:"t_{peer}",name:"signed peer time",description:"Syn 또는 SynAck transcript에 포함되고 identity signature로 인증된 millisecond timestamp입니다."},{symbol:"t_{min}",name:"oldest accepted time",description:"Replay window보다 오래된 handshake를 거절하는 하한입니다."},{symbol:"t_{max}",name:"future limit",description:"허용 clock skew를 넘는 미래 timestamp를 거절하는 exclusive 상한입니다."}]} assumptions={["세 값은 같은 millisecond clock convention과 reference interval을 사용합니다.","Range는 deployment가 현재 time과 max age/skew 정책에서 계산합니다.","Timestamp gate는 nonce uniqueness와 identity signature를 대신하지 않습니다.","Clock rollback·restart에서는 persisted session/counter policy를 별도로 검사합니다."]} interpretation="허용 범위가 [95,000,102,000)ms라면 100,000ms는 통과하고 94,000ms와 102,000ms는 거절합니다. 범위를 넓히면 availability는 좋아질 수 있지만 replay window도 커집니다."/><div id="paper-commonware-handshake-v2026"><CitationBlock source="Commonware v2026.7.0 — cryptography/src/handshake.rs" citeKey={1} type="code" href="https://github.com/commonwarexyz/monorepo/blob/v2026.7.0/cryptography/src/handshake.rs"><p><strong>문제:</strong> 미리 알려진 두 identity가 untrusted transport 위에서 authenticated encrypted session을 만듭니다.</p><p><strong>기여:</strong> Syn·SynAck·Ack, timestamp range, transcript signatures, key confirmation과 directional AEAD ciphers를 구현합니다.</p><p><strong>전제:</strong> v2026.7.0 cryptography primitives, expected public keys, secure RNG와 deployment clock policy를 사용합니다.</p><p><strong>근거 범위:</strong> Three-message authenticated key exchange와 ordered per-direction cipher lifecycle입니다.</p><p><strong>말하지 않는 것:</strong> Application authorization, artifact content validity, durable message delivery를 보장하지 않습니다.</p></CitationBlock></div></section>
  <section id="channel-quota" className="space-y-6"><header><p className="text-sm font-semibold text-primary">02 · channel과 quota</p><h2 className="mt-2 text-2xl font-bold">Authenticated peer의 모든 message를 같은 권한·queue로 취급하지 않는다</h2></header><p>P2P network는 등록한 numeric channel마다 sender/receiver, receive rate quota와 bounded backlog를 만듭니다. Encoded data에는 channel과 message length가 붙고 max message size를 넘거나 미등록 channel이면 handler에 도달시키지 않습니다. Channel 10의 metadata parser와 channel 20의 range-block parser는 서로 다른 authorization과 resource budget을 가질 수 있습니다.</p><p>High/low priority relay도 각각 bounded mailbox입니다. <code>Feedback::Ok</code>는 local queue admission이고, full이면 <code>Rejected</code>, receiver가 사라졌으면 <code>Closed</code>입니다. Priority는 remote receipt나 bandwidth reservation이 아니므로 retry key와 deadline을 application receipt에 넣어야 합니다.</p><CommonwareChannelViz/><div id="paper-commonware-p2p-v2026"><CitationBlock source="Commonware v2026.7.0 — p2p authenticated lookup network" citeKey={2} type="code" href="https://github.com/commonwarexyz/monorepo/tree/v2026.7.0/p2p/src/authenticated/lookup"><p><strong>문제:</strong> Fixed authenticated peer set에 channel별 resource isolation과 bounded message delivery를 제공합니다.</p><p><strong>기여:</strong> Channel registration, rate quota, backlog, max message size, handshake limits와 routing actors를 구현합니다.</p><p><strong>전제:</strong> v2026.7.0 lookup network configuration과 known peer address/identity mapping을 사용합니다.</p><p><strong>근거 범위:</strong> Node-local dial/listen, authenticated channel admission과 bounded routing lifecycle입니다.</p><p><strong>말하지 않는 것:</strong> Byzantine application payload validity, remote processing ack, consensus ordering을 제공하지 않습니다.</p></CitationBlock></div><div id="paper-commonware-mux-v2026"><CitationBlock source="Commonware v2026.7.0 — p2p/utils/mux.rs · authenticated/relay.rs" citeKey={3} type="code" href="https://github.com/commonwarexyz/monorepo/blob/v2026.7.0/p2p/src/utils/mux.rs"><p><strong>문제:</strong> 한 P2P sender/receiver를 독립 subchannels과 priority queues로 나누고 slow consumer를 격리합니다.</p><p><strong>기여:</strong> Dynamic route registration, per-subchannel mailbox, non-blocking drop와 high/low admission feedback을 구현합니다.</p><p><strong>전제:</strong> Unique channel IDs, bounded mailbox size와 continuously polled mux actor를 사용합니다.</p><p><strong>근거 범위:</strong> Local message demultiplexing, overflow handling과 deregistration cleanup입니다.</p><p><strong>말하지 않는 것:</strong> Reliable retransmission, global stream ordering, remote durable acceptance를 보장하지 않습니다.</p></CitationBlock></div></section>
  <section id="release" className="space-y-6"><header><p className="text-sm font-semibold text-primary">03 · failure와 release</p><h2 className="mt-2 text-2xl font-bold">Handshake failure, queue rejection과 payload rejection을 다른 retry class로 둔다</h2></header><p>Wrong identity·signature·confirmation, stale timestamp와 AEAD tag failure는 session을 닫고 plaintext fallback하지 않습니다. Handshake rate/concurrency limit이나 full mailbox는 backoff 가능한 admission failure지만, unauthorized channel과 malformed payroll block은 application reject로 분리합니다. Retry할 때는 같은 artifact/range operation ID로 duplicate side effect를 막습니다.</p><p>Release test는 role swap·replay·clock skew·counter rollback, oversized/unregistered channel, slow subchannel, high/low overflow와 shutdown cleanup을 재현합니다. Authenticated peer key, transcript/session revision, channel, local feedback, bytes/digest와 application receipt가 모두 맞을 때만 artifact를 publish합니다.</p><h3 className="text-xl font-semibold">이 글만으로 풀어야 하는 10문제</h3><p>기초 6문제는 Syn/SynAck/Ack, timestamp range, directional cipher, channel ID, quota와 Feedback 의미를 확인합니다. 심화 4문제는 replay/counter rollback, slow channel 격리, overflow retry와 end-to-end release test를 설계하게 합니다.</p></section>
</article>; }
