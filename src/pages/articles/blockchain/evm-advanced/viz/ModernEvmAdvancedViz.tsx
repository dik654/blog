const rows = [
  ["CALL", "callee", "caller", "callee", "allowed"],
  ["DELEGATECALL", "caller storage", "original caller", "unchanged", "allowed"],
  ["STATICCALL", "callee", "caller", "0", "forbidden"],
] as const;

export function CallContextMatrixViz(){return <figure className="rounded-xl border border-border bg-card p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">Opcode가 바꾸는 execution context</figcaption><div className="overflow-x-auto"><table className="w-full min-w-[620px] border-collapse text-left text-sm"><thead><tr className="border-b border-border text-xs text-muted-foreground"><th className="p-3">opcode</th><th className="p-3">storage owner</th><th className="p-3">msg.sender</th><th className="p-3">msg.value</th><th className="p-3">state write</th></tr></thead><tbody>{rows.map(r=><tr key={r[0]} className="border-b border-border last:border-0">{r.map((v,i)=><td key={v+i} className={i===0?"p-3 font-mono font-semibold":"p-3 text-muted-foreground"}>{v}</td>)}</tr>)}</tbody></table></div><p className="mt-3 text-xs leading-5 text-muted-foreground">DELEGATECALL의 code owner와 storage owner가 다르다는 점이 proxy의 힘이자 위험입니다.</p></figure>}

export function CreateLifecycleViz(){return <figure className="rounded-xl border border-border bg-card p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">CREATE 계열: init code와 runtime code를 분리</figcaption><div className="grid gap-3 md:grid-cols-[1fr_auto_1fr_auto_1fr] md:items-center">{[["candidate address","nonce 또는 salt·init hash"],["run init code","child frame + journal"],["store return bytes","runtime code"]].map((x,i)=><div key={x[0]} className="contents"><div className="rounded-lg border border-border bg-background p-4"><p className="text-xs font-semibold text-primary">0{i+1}</p><p className="mt-2 font-semibold">{x[0]}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{x[1]}</p></div>{i<2&&<span aria-hidden className="hidden text-muted-foreground md:block">→</span>}</div>)}</div></figure>}
