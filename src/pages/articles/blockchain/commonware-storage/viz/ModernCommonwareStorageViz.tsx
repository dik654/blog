const Card=({tag,title,text}:{tag:string;title:string;text:string})=><div className="min-w-0 rounded-lg border border-border bg-card p-4"><p className="text-xs font-semibold text-primary">{tag}</p><p className="mt-1 font-semibold">{title}</p><p className="mt-2 break-words text-sm leading-6 text-muted-foreground">{text}</p></div>;
export function StorageBatchViz(){return <figure data-viz="commonware-storage-batch" className="not-prose min-w-0 rounded-xl border border-border bg-muted/20 p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">Mutation은 speculative batch에서 root를 계산한 뒤 winning branch만 durable하게 만든다</figcaption><div className="grid gap-4 md:grid-cols-4"><Card tag="01 append" title="operations" text="Alice와 Bob key update를 append-only log에 stage합니다."/><Card tag="02 compute" title="merkleize" text="현재 state에 resolve해 candidate root를 계산합니다."/><Card tag="03 choose" title="apply batch" text="Parent chain이 아직 DB prefix와 맞는 branch만 적용합니다."/><Card tag="04 durable" title="commit·sync" text="Flush receipt 뒤에만 root를 external success와 연결합니다."/></div></figure>}
export function ProofBoundaryViz(){return <figure data-viz="commonware-storage-proof" className="not-prose min-w-0 rounded-xl border border-border bg-card p-4 sm:p-6"><figcaption className="mb-4 text-sm font-semibold">Any proof와 Current proof는 답하는 질문이 다르다</figcaption><div className="grid gap-4 sm:grid-cols-3"><Card tag="MMR" title="append-only position" text="Stable leaf location과 peak path로 log inclusion을 검증합니다."/><Card tag="Any" title="ever had value?" text="Key가 과거 어느 시점에 value를 가졌다는 증거를 제공합니다."/><Card tag="Current" title="still current?" text="Activity bitmap을 ops tree에 graft해 현재 value 여부까지 한 root에 묶습니다."/></div></figure>}
