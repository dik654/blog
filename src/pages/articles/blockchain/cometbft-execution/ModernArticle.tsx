import ExplainedFormula from "@/components/ui/explained-formula";
import { CitationBlock } from "@/components/ui/citation-block";
import { ApplyBlockPipelineViz, CrashCutsViz } from "./viz/ModernExecutionViz";

export default function ModernCometBFTExecutionArticle() {
  return <article className="space-y-14">
    <section id="overview" className="space-y-6">
      <header className="space-y-3"><p className="text-sm font-semibold text-primary">CometBFT v0.40.0 구현 읽기</p><h2 className="text-3xl font-bold tracking-tight">ApplyBlock은 결정된 block을 검증·실행·영속화하는 orchestration 경계다</h2></header>
      <p className="text-lg leading-8 text-foreground/90">Consensus가 height 42의 block을 결정했다고 해서 <code>alice→bob 10</code>의 balance 변화가 이미 durable한 것은 아닙니다. <code>BlockExecutor.ApplyBlock</code>은 이전 CometBFT State에 대해 block을 검증하고, application에 FinalizeBlock을 호출해 결과를 계산하며, result를 저장한 뒤 application Commit과 mempool Update, 새 State 저장까지 연결합니다.</p>
      <p>이 순서는 한 database transaction으로 묶이지 않습니다. 그래서 단계별 receipt와 replay rule이 구현의 일부입니다. ABCI method의 authority와 deterministic application 조건은 <a className="text-primary hover:underline" href="/blockchain/cometbft-abci#finalize-commit">ABCI++ 정본</a>, protocol object와 commit certificate는 <a className="text-primary hover:underline" href="/blockchain/cometbft-types#vote-commit">types 정본</a>에서 먼저 또는 필요할 때 이어 읽을 수 있습니다.</p>
      <ApplyBlockPipelineViz />
    </section>

    <section id="validate-block" className="space-y-6">
      <header><p className="text-sm font-semibold text-primary">01 · ValidateBlock</p><h2 className="mt-2 text-2xl font-bold">실행 전에 block이 바로 다음 state transition인지 증명한다</h2></header>
      <p>Validation은 State를 mutate하지 않습니다. 대신 chain ID, 예상 height, LastBlockID, header AppHash·validator hash·consensus params hash, transaction DataHash, last commit의 signature와 historical voting power, block time과 evidence byte limit을 검사합니다. EvidencePool도 block에 실린 evidence가 아직 유효하고 중복 처리되지 않았는지 확인합니다.</p>
      <p>가장 쉬운 off-by-one은 height입니다. Genesis 뒤 첫 block은 <code>InitialHeight</code>여야 하고, 그 다음부터 block height는 <code>State.LastBlockHeight+1</code>이어야 합니다. Header의 AppHash는 현재 block을 실행하기 전 State.AppHash와 같아야 하므로, block 42의 FinalizeBlock에서 새로 나온 AppHash와 비교하면 정상 block도 거절됩니다.</p>
      <ExplainedFormula question="Block h가 State snapshot 바로 다음에 실행할 유일한 후보인지 어떤 불변식으로 확인하는가?" idea={<>높이, 이전 block identity와 이전 application commitment가 동시에 이어져야 합니다. 셋 중 하나라도 다르면 다른 history나 잘못된 높이를 현재 State 위에 실행하려는 것입니다.</>} formula={String.raw`\begin{aligned}h_B&=h_S+1\\B.\mathrm{LastBlockID}&=S.\mathrm{LastBlockID}\\B.\mathrm{AppHash}&=S.\mathrm{AppHash}\end{aligned}`} terms={[{symbol:"h_B",name:"block height",description:"검증하려는 block header의 height입니다."},{symbol:"h_S",name:"state height",description:"State.LastBlockHeight입니다. 최초 block은 InitialHeight 예외를 적용합니다."},{symbol:"LastBlockID",name:"history link",description:"직전 block hash와 part-set header를 묶는 identity입니다."},{symbol:"AppHash",name:"application link",description:"직전 committed application state에 대한 application-defined commitment입니다."}]} assumptions={["같은 chain ID와 pinned v0.40.0 field semantics를 사용합니다.","LastCommit·validator hash·params·data·time·evidence 검증도 별도로 통과해야 합니다.","최초 block은 hS+1 대신 configured InitialHeight 규칙을 적용합니다.","AppHash equality는 application transition의 business validity를 대신하지 않습니다."]} interpretation="State가 41인데 block height가 43이거나 LastBlockID가 다른 branch를 가리키면 실행하지 않습니다. Height와 link가 맞아도 commit signature나 evidence가 틀리면 역시 fail closed합니다." />
      <div id="paper-cometbft-validation-v040"><CitationBlock source="CometBFT v0.40.0 — state/validation.go" citeKey={1} type="code" href="https://github.com/cometbft/cometbft/blob/v0.40.0/state/validation.go"><p><strong>문제:</strong> 결정 후보 block이 현재 chain state의 정확한 다음 transition인지 검증합니다.</p><p><strong>기여:</strong> height·history·commitment·commit·time·evidence 조건의 fail-fast validation을 구현합니다.</p><p><strong>전제:</strong> v0.40.0 State와 historical validator·consensus params가 정확히 로드됩니다.</p><p><strong>근거 범위:</strong> CometBFT block/state structural·cryptographic validity입니다.</p><p><strong>말하지 않는 것:</strong> application transaction의 business success나 Commit durability를 보장하지 않습니다.</p></CitationBlock></div>
    </section>

    <section id="execute-block" className="space-y-6">
      <header><p className="text-sm font-semibold text-primary">02 · FinalizeBlock과 transition</p><h2 className="mt-2 text-2xl font-bold">같은 ordered input은 같은 result·validator update·AppHash를 내야 한다</h2></header>
      <p>Validation 뒤 BlockExecutor는 block hash, height·time, proposer, decided last commit, misbehavior와 ordered transaction bytes를 <code>RequestFinalizeBlock</code>으로 application에 보냅니다. 응답에는 transaction마다 하나의 result, validator updates, consensus parameter updates, events와 AppHash가 들어갑니다. v0.40.0 구현은 transaction 수와 result 수가 다르면 즉시 오류를 냅니다.</p>
      <p><code>updateState</code>는 이 response를 이용해 LastBlockHeight·ID·time, validator snapshots와 consensus parameters, LastResultsHash를 다음 State로 이동시킵니다. 다만 최종 AppHash field는 application Commit 뒤에 채워 저장합니다. Application은 같은 previous committed state와 같은 block/context에서 같은 response를 만들어야 하며 local clock, random map iteration, unpinned remote API가 result에 영향을 주면 replicas가 다음 header commitment에서 갈라질 수 있습니다.</p>
      <p>이 deterministic transition 식과 candidate/committed state 경계는 <a className="text-primary hover:underline" href="/blockchain/cometbft-abci#finalize-commit">ABCI++ 글의 S<sub>h+1</sub>=F(S<sub>h</sub>,B<sub>h</sub>,C<sub>h</sub>) 정본</a>을 재사용합니다. ApplyBlock은 그 계산을 호출하고 결과 개수·update validity·저장 순서를 검사하는 orchestration owner입니다.</p>
    </section>

    <section id="save-state" className="space-y-6">
      <header><p className="text-sm font-semibold text-primary">03 · SaveState와 crash replay</p><h2 className="mt-2 text-2xl font-bold">result를 먼저 저장하고 Commit 뒤 State를 저장해 crash 위치를 판별한다</h2></header>
      <p>v0.40.0 <code>applyBlock</code>는 FinalizeBlock response를 height별·latest recovery key로 먼저 저장합니다. 이어 validator update를 검증하고 State 후보를 계산한 다음 mempool을 lock·flush하고 application <code>Commit</code>을 호출합니다. Application은 Commit이 반환되기 전에 자기 state를 durable하게 저장해야 합니다. 그 뒤 mempool Update를 비동기로 시작하고, response AppHash를 State에 넣어 synchronous batch로 저장합니다.</p>
      <p>이 ordering 때문에 application Commit 뒤 State 저장 전에 crash해도 복구할 수 있습니다. Restart handshake가 application h, State h−1, BlockStore h를 보면 저장해 둔 FinalizeBlock response와 application AppHash를 대조한 뒤 mock application 경로로 State만 다시 만듭니다. 반대로 application이 h−1이면 real application에 block을 replay해야 합니다. Timeout 하나만 보고 FinalizeBlock·Commit을 다시 호출하면 transaction이나 external effect를 중복 적용할 수 있습니다.</p>
      <CrashCutsViz />
      <div id="paper-cometbft-execution-v040"><CitationBlock source="CometBFT v0.40.0 — state/execution.go · consensus/replay.go" citeKey={2} type="code" href="https://github.com/cometbft/cometbft/blob/v0.40.0/state/execution.go"><p><strong>문제:</strong> block validation, application execution과 서로 다른 durable stores 사이의 crash window를 복구합니다.</p><p><strong>기여:</strong> ApplyBlock ordering, result-before-Commit 저장, mempool lock·Update와 handshake replay 분기를 구현합니다.</p><p><strong>전제:</strong> application Commit durability contract, local block/result 보유와 matching chain/config를 전제로 합니다.</p><p><strong>근거 범위:</strong> v0.40.0 node/application state transition orchestration과 local restart입니다.</p><p><strong>말하지 않는 것:</strong> webhook·payment 같은 external effect의 exactly-once, 손상된 DB의 자동 복구, arbitrary rollback을 보장하지 않습니다.</p></CitationBlock></div>
      <p>Release gate는 정상 control과 같은 block trace를 Finalize 전, result 저장 뒤, Commit 뒤, State 저장 뒤에 각각 crash시킵니다. 재시작 후 height·AppHash·tx result·validator set·balance가 control과 같고, 같은 transaction이 한 번만 반영되며, 잘못된 LastBlockID·AppHash·result count는 모두 거절되어야 합니다. External effect는 block identity를 idempotency key로 삼는 outbox와 별도 reconciliation을 둡니다.</p>
      <h3 className="text-xl font-semibold">이 글만으로 풀어야 하는 10문제</h3><p>기초 6문제는 validation 입력, height/AppHash 관계, Finalize response, updateState, Commit과 저장 순서를 확인합니다. 심화 4문제는 nondeterministic application 반례, result count mismatch, 네 crash cut과 external-effect reconciliation release gate를 설계하게 합니다.</p>
    </section>
  </article>;
}
