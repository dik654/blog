const Card = ({ label, title, text, tone = "border-border" }: { label: string; title: string; text: string; tone?: string }) => <div className={`min-w-0 rounded-lg border ${tone} bg-background p-4`}><p className="text-[11px] font-bold uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 break-words font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div>;
export function BroadcastReceiptViz() { return <figure data-viz="commonware-broadcast-receipt" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">payroll-report-v1: local broadcast와 verified receive는 다른 receipt다</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 md:grid-cols-4"><Card label="1 · enqueue" title="Feedback::Ok" text="Local mailbox가 요청을 받아들였습니다."/><Card label="2 · network" title="authenticated peer" text="Commonware P2P가 sender public key와 bytes를 전달합니다." tone="border-sky-500/40"/><Card label="3 · decode/cache" title="Digestible message" text="Codec과 digest로 typed item을 cache에 넣습니다."/><Card label="4 · application" title="verified receipt" text="Expected digest·schema·sender policy 뒤에만 publish합니다." tone="border-emerald-500/40"/></div></figure>; }
export function BroadcastCacheViz() { return <figure data-viz="commonware-broadcast-cache" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">Peer별 deque와 digest refcount가 cache residence를 결정한다</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 sm:grid-cols-3"><Card label="peer A deque" title="{d7, d3}" text="d7을 가장 최근에 보냈습니다."/><Card label="peer B deque" title="{d7, d5}" text="같은 d7 item은 Arc 하나로 공유됩니다."/><Card label="global count" title="c(d7)=2" text="A에서 evict해도 1, B에서도 evict할 때 0이 되어 item을 제거합니다." tone="border-emerald-500/40"/></div></figure>; }
