export const coldVsHotCode = `Filecoin 스토리지 진화:

Cold Storage (초기 Filecoin):
  데이터 → PoRep 봉인(Sealing) → 봉인된 섹터
  → 검색 시: 봉인 해제(Unseal) 필요 (~수 시간)
  → 아카이브 용도에 적합, 실시간 접근 불가

Hot Storage (현재):
  데이터 → 봉인된 섹터 + 원본 데이터 캐시
  → 검색 시: 원본에서 즉시 HTTP 전송
  → Boost의 핵심 혁신

┌────────────────────────────────────────────┐
│          Storage Provider 노드              │
│                                            │
│  ┌──────────────────────────────────────┐  │
│  │ Hot Storage (즉시 접근)               │  │
│  │  → 원본 데이터 (CAR 파일)             │  │
│  │  → Piece Store (인덱스)               │  │
│  │  → HTTP 서버 (직접 전송)              │  │
│  └──────────────────────────────────────┘  │
│                                            │
│  ┌──────────────────────────────────────┐  │
│  │ Cold Storage (봉인 저장)              │  │
│  │  → 봉인된 섹터 (PoRep 증명용)        │  │
│  │  → WindowPoSt 검증 대상               │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘

핫 스토리지의 이점:
  검색 지연:  수 시간 → 밀리초
  프로토콜:   Graphsync → HTTP (표준)
  비용:       추가 저장 공간 필요 (원본 유지)
  호환성:     기존 CDN/웹 인프라와 호환`;

export const boostArchCode = `Boost = Filecoin Storage Provider의 딜 관리 소프트웨어

기존 go-fil-markets (레거시):
  → Lotus 내장, 모놀리식
  → Graphsync 기반 검색 (비표준)
  → 확장성 제한

Boost (현재 표준):
  → Lotus에서 분리된 독립 서비스
  → HTTP 기반 검색 (표준 호환)
  → 모듈식 아키텍처

Boost 핵심 컴포넌트:

┌────────────────────────────────────────┐
│           Boost Daemon                  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ Deal Engine                      │  │
│  │  → 딜 제안 수신 & 검증            │  │
│  │  → 오프라인/온라인 딜 관리         │  │
│  │  → 딜 상태 추적 (State Machine)   │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ booster-http (검색 서버)          │  │
│  │  → /piece/ 전체 피스 검색         │  │
│  │  → /ipfs/ Trustless Gateway      │  │
│  │  → CAR + raw IPLD 블록 지원      │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │ booster-bitswap (IPFS 호환)      │  │
│  │  → Filecoin SP를 IPFS 노드처럼   │  │
│  │  → 기존 IPFS 노드와 투명하게 교환 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ Local Index Directory (LID)       │  │
│  │  → 블록별 위치 메타데이터 저장     │  │
│  │  → LevelDB (소규모) 또는          │  │
│  │    YugabyteDB (>1PiB, 수만 딜)   │  │
│  │  → 서브피스 블록 레벨 검색 지원    │  │
│  │  → 다중 boostd 인스턴스 공유 가능 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ Boost UI (웹 대시보드)            │  │
│  │  → 딜 현황 모니터링               │  │
│  │  → 저장 공간 관리                  │  │
│  │  → 성능 메트릭                    │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘`;
