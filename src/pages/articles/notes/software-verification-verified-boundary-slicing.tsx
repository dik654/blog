// 자동 생성 wrapper — fundamentals-to-blog.ts 가 매번 재생성. 수정 금지.
import MarkdownNote from '@/components/MarkdownNote';
import raw from '@/content/notes/software-verification-verified-boundary-slicing.md?raw';

export default function Note() {
  return <MarkdownNote raw={raw} />;
}
