const rows=[["SATA","ATA/AHCI·NCQ","point-to-point"],["SAS","SCSI·SAS","dual-port·expander"],["NVMe","NVMe queues","PCIe·fabric"]] as const;
export function StorageStackViz(){return <figure data-viz="storage-stack" className="not-prose my-9 rounded-xl border border-border bg-card p-5"><figcaption><p className="text-xs font-semibold text-primary">같은 축으로 비교</p><p className="mt-1 font-bold">매체 이름이 아니라 command·transport·topology를 나눈다</p></figcaption><div data-viz-canvas className="mt-5 grid gap-3">{rows.map(([a,b,c])=><div key={a} className="grid min-w-0 gap-2 rounded-lg border border-border bg-background p-3 sm:grid-cols-[5rem_1fr_1fr]"><strong className="text-sm">{a}</strong><span className="text-xs leading-5 text-muted-foreground">{b}</span><span className="text-xs leading-5 text-muted-foreground">{c}</span></div>)}</div></figure>}
export function TierPlacementViz(){return <figure data-viz="storage-tier-placement" className="not-prose my-9 rounded-xl border border-border bg-card p-5"><figcaption><p className="text-xs font-semibold text-primary">한 workload, 다른 실패 비용</p><p className="mt-1 font-bold">scratch·metadata·durable data를 같은 drive 정책에 넣지 않는다</p></figcaption><div data-viz-canvas className="mt-5 grid gap-4 md:grid-cols-3">{[["Scratch","재생성 가능","throughput"],["Metadata·log","작지만 중요","tail·durability"],["Durable data","복구 비용 큼","failure domain"]].map(([a,b,c])=><div key={a} className="rounded-lg border border-border bg-background p-4"><p className="text-sm font-bold">{a}</p><p className="mt-2 text-xs text-muted-foreground">{b}</p><p className="mt-1 text-xs font-semibold text-primary">{c}</p></div>)}</div></figure>}
