const stages = [["Workload", "용량·SLO"], ["Topology", "lane·channel"], ["Operate", "BMC·hot-swap"], ["Release", "고장 주입"]] as const;
export function ServerDecisionViz() { return <figure data-viz="server-decision" className="not-prose my-9 overflow-hidden rounded-xl border border-border bg-card"><figcaption className="border-b border-border px-5 py-4"><p className="text-xs font-semibold text-primary">부품명이 아니라 서비스 경로</p><p className="mt-1 font-bold">요구량을 적고 연결·정비·복구까지 차례로 확인한다</p></figcaption><div data-viz-canvas className="grid gap-4 p-5 sm:grid-cols-2 lg:grid-cols-4">{stages.map(([a,b],i)=><div key={a} className="relative min-w-0 rounded-lg border border-border bg-background p-4"><span className="text-[11px] font-bold text-primary">0{i+1}</span><p className="mt-2 text-sm font-bold">{a}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{b}</p>{i<3?<span aria-hidden className="absolute -right-3 top-1/2 hidden -translate-y-1/2 bg-card px-1 text-muted-foreground lg:block">→</span>:null}</div>)}</div></figure> }
export function PlatformPathViz() { return <figure data-viz="platform-path" className="not-prose my-9 rounded-xl border border-border bg-card p-5"><figcaption><p className="text-xs font-semibold text-primary">한 장치의 실제 경로</p><p className="mt-1 font-bold">슬롯 개수만으로는 lane·NUMA 병목을 알 수 없다</p></figcaption><div data-viz-canvas className="mt-5 grid gap-4 md:grid-cols-[1fr_auto_1fr_auto_1fr] md:items-center">{[["CPU socket", "memory channels"], ["Root·switch", "lane·bifurcation"], ["GPU·NVMe·NIC", "negotiated width"]].map(([a,b],i)=><div key={a} className="contents"><div className="min-w-0 rounded-lg border border-border bg-background p-4"><p className="text-sm font-bold">{a}</p><p className="mt-1 text-xs text-muted-foreground">{b}</p></div>{i<2?<span aria-hidden className="hidden text-muted-foreground md:block">→</span>:null}</div>)}</div></figure> }
