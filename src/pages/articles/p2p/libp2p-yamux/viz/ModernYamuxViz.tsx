const Card = ({ label, title, text, tone = "border-border" }: { label: string; title: string; text: string; tone?: string }) => <div className={`min-w-0 rounded-lg border ${tone} bg-background p-4`}><p className="text-[11px] font-bold uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 break-words font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div>;
export function YamuxStackViz() { return <figure data-viz="yamux-stack" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">payroll-report-v1: 한 secure connection 안의 독립 stream</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 md:grid-cols-4"><Card label="1 · transport" title="TCP connection" text="Peer까지 ordered byte stream을 만듭니다."/><Card label="2 · identity" title="Noise secure channel" text="Expected PeerId와 session keys를 확인합니다." tone="border-sky-500/40"/><Card label="3 · mux" title="Yamux streams" text="metadata와 piece 요청을 stream ID별로 분리합니다." tone="border-emerald-500/40"/><Card label="4 · app" title="protocol + digest" text="각 stream의 protocol과 artifact hash를 검증합니다."/></div><p className="mt-4 text-xs leading-5 text-muted-foreground">Yamux는 암호화나 파일 검증을 하지 않습니다. 이미 인증된 byte connection을 여러 substream으로 나눕니다.</p></figure>; }
export function YamuxBackpressureViz() { return <figure data-viz="yamux-backpressure" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">Stream credit와 inbound admission은 서로 다른 두 제한이다</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 sm:grid-cols-3"><Card label="stream A" title="64 KiB credit" text="48 KiB DATA 뒤에는 16 KiB만 남습니다." tone="border-emerald-500/40"/><Card label="application" title="read → update" text="읽은 만큼 WINDOW_UPDATE를 보내 remote credit를 복구합니다."/><Card label="connection" title="bounded inbound queue" text="Caller가 느리면 buffer가 차고 새 inbound stream은 reset/drop됩니다." tone="border-amber-500/40"/></div></figure>; }
