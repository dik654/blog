const Box = ({ step, title, detail, accent = "border-border" }: { step: string; title: string; detail: string; accent?: string }) => <div className={`min-w-0 rounded-lg border ${accent} bg-background p-4`}><p className="text-[11px] font-bold uppercase tracking-wider text-muted-foreground">{step}</p><p className="mt-2 break-words font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{detail}</p></div>;

export function KuboReceiptViz() { return <figure data-viz="kubo-receipts" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">payroll-report-v1: 서로 대체할 수 없는 네 가지 receipt</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 md:grid-cols-4"><Box step="1 · identity" title="CID" detail="bytes가 주소와 일치하는지 검증합니다." accent="border-sky-500/40"/><Box step="2 · routing" title="provider 후보" detail="이 peer에게 물어볼 근거일 뿐 possession proof는 아닙니다."/><Box step="3 · exchange" title="Bitswap block" detail="실제 block bytes를 받아 CID로 다시 검증합니다." accent="border-emerald-500/40"/><Box step="4 · retention" title="pin receipt" detail="이 node의 GC root에 포함되었다는 local 보존 계약입니다."/></div></figure>; }

export function KuboGcViz() { return <figure data-viz="kubo-gc" className="my-8 min-w-0 rounded-xl border border-border/70 bg-muted/15 p-4 sm:p-6"><figcaption className="mb-5 text-sm font-semibold">작은 blockstore에서 pin/GC를 계산하는 예</figcaption><div data-viz-canvas className="grid min-w-0 gap-3 sm:grid-cols-3"><Box step="blockstore B" title="{A, B, C, D, E}" detail="현재 local store에 있는 모든 block입니다."/><Box step="protected R" title="{A, B, C, E}" detail="pin root와 reachable DAG, MFS·internal root에서 칠해진 집합입니다." accent="border-emerald-500/40"/><Box step="delete B ∖ R" title="{D}" detail="보호 집합 밖에 있어 sweep 대상으로 남습니다." accent="border-rose-500/40"/></div><p className="mt-4 text-xs leading-5 text-muted-foreground">Pin 삭제는 즉시 block 삭제와 같지 않습니다. 다음 GC에서 다른 root가 D를 보호하는지 다시 계산한 뒤 제거합니다.</p></figure>; }
