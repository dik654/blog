export const pipelineOverviewCode = `ZK 증명 생성 파이프라인 (공통 구조):

  Witness (CPU)          NTT (GPU)             MSM (GPU)            Proof
  ┌──────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────┐
  │ 제약 조건 │────→│ 다항식 평가  │────→│ 타원곡선     │────→│ π = (A,B,│
  │ 풀이     │     │ at ω^i       │     │ 스칼라 곱    │     │    C)    │
  │ (순차적) │     │ O(n log n)   │     │ O(n)         │     │ 256 bytes│
  └──────────┘     └──────────────┘     └──────────────┘     └──────────┘
       │                  │                    │
    CPU only          GPU 20-30%           GPU 60-70%
                     of total time        of total time

  시간 비중 (2^20 constraints 기준):
  ┌──────────────────────────────────────────────────────────────┐
  │  MSM ██████████████████████████████████████░░░░  ~65%       │
  │  NTT ████████████████░░░░░░░░░░░░░░░░░░░░░░░░  ~25%       │
  │  기타 ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  ~10%       │
  └──────────────────────────────────────────────────────────────┘`;

export const speedupTableRows = [
  { op: 'MSM (2^23 points)', cpu: '~45s', gpu: '~0.5s', speedup: '~90x' },
  { op: 'NTT (2^23 degree)', cpu: '~8s', gpu: '~0.12s', speedup: '~65x' },
  { op: 'Groth16 전체 (2^20)', cpu: '~120s', gpu: '~3s', speedup: '~40x' },
  { op: 'PLONK 전체 (2^20)', cpu: '~180s', gpu: '~5s', speedup: '~36x' },
];
