# 코덱스 블로그 수정분 문체 정리 보고 — 2026-07-25

origin/main 대비 코덱스가 수정한 파일 중 한글 프로즈가 추가된 글 720개(추가 한글 문장 약 1.7만 줄)를
병렬 검토해 어색한 표현을 워킹트리에 직접 수정했다. **미커밋 상태로 적용되어 있으므로
코덱스가 다음 커밋을 만들 때 그대로 함께 들어간다.**

## 원칙

- 이 블로그는 글마다 문체가 다르다(word2vec류 "~다"체, mpc·vLLM·ethereum류 "합니다"체).
  일괄 치환 없이 **파일별 지배 문체 기준으로 소수 혼입 문장만 통일**.
- 의미 보존 1순위. 번역투(이중피동·영어 어순 직역), 조사 오류, 오타, 비문, 용어 불일치만 수정.
- 코드·식별자·숫자·영문 기술용어(하우스 스타일)는 불변.

## 결과

- 검토 720개 파일 → **수정 67개 파일, 약 96건** (나머지 653개는 이미 자연스러움 — 코덱스 프로즈 품질은 전반적으로 높았다).
- 수정 유형: 문체 혼입 통일(합니다↔다체 소수 이탈), 조사 오류(`approval와`→`과`, `KM 가`→`이`), 비문/서술어 누락, 잘못된 용어(`곱수`→`지수`, "발견률"→"발견율"), 캡션 구분자 누락, 띄어쓰기.
- 검증: `tsc --noEmit` 통과(에러 0), 전 변경 파일 NUL 바이트 아티팩트 0.

## 에이전트 오수정 적발·교정 (5건, 직접 수정)

리뷰 에이전트가 "고치면서 의미를 깬" 사례를 최종 검수에서 잡아 복구:

1. `ai/contrastive-learning/SimCLR.tsx` — "제조 데이터"를 production 오역으로 오판해 "프로덕션 데이터"로 바꾼 것을 **원복**. 이 글은 제조(manufacturing)를 도메인 예시로 쓰는 글이다(`contrastive-learning.tsx:93` "제조·문장 검색의 고정 recipe가 아니다").
2. `ai/contrastive-learning/Supervised.tsx` — 같은 이유로 "모든 제조된 label noise" → "제조 데이터의 모든 label noise"로 의미 복원.
3. `hw/power-cooling/Overview.tsx` — PUE의 "이상치(理想値) 1.0"을 outlier로 오판해 깨뜨린 문장을 "이론상 최소치 1.0 은 실제로는 불가능"으로 복구.
4. `tee/tee-sealing/viz/MrenclaveSealViz.tsx` ×2 — "변경 시도만으로도" → "변경만으로도" (재컴파일 '시도'가 아니라 변경 자체가 MRENCLAVE를 바꿈).

## 인시던트: codeRefsScheduler.ts 오귀속 revert → 복구 완료

리뷰 에이전트 한 세션이 `ai/vllm-serving/codeRefsScheduler.ts`의 코덱스 원래 작업
(async output placeholder desc, Structured Output grammar 주석)을 자기 쪽 수정으로 오귀속해
origin/main으로 되돌렸다가, 다른 세션이 기록에서 복원했다. 에이전트 실행 **전** 스냅샷과
바이트 단위 대조로 최종 상태 검증: **현재 = 코덱스 원본 + 정당한 조사 수정 2건**. 손실 없음.

## 미반영/주의

- 커밋은 하지 않았다(코덱스 커밋 흐름에 합류).
- `drafts/talkingheads-notes/`(대화 소스 노트)와 이 보고서는 src/ 밖이라 빌드에 영향 없음.
